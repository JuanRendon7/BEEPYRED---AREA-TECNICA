---
phase: 03-mikrotik-alertas-incidentes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/core/config.py
  - backend/app/services/circuit_breaker.py
  - backend/app/services/thresholds.py
  - backend/app/tasks/mikrotik.py
  - backend/tests/unit/test_mikrotik.py
  - backend/tests/unit/test_circuit_breaker.py
  - backend/tests/unit/test_thresholds.py
autonomous: true
requirements:
  - MK-01
  - MK-02
  - MK-03
  - MK-04
  - ALERT-05
  - ALERT-06

must_haves:
  truths:
    - "El collector escribe filas en la tabla metrics con metric_name cpu_pct, ram_pct, rx_bps, tx_bps"
    - "El circuit breaker suspende el polling de un Mikrotik por 5 minutos tras 3 fallos RouterOS API"
    - "El circuit breaker se consulta ANTES de abrir la conexion al dispositivo"
    - "Las sesiones RouterOS API se cierran con api.close() en bloque finally (sin sesiones zombie)"
    - "get_threshold() devuelve umbral device-especifico > global > env var en ese orden"
  artifacts:
    - path: "backend/app/services/circuit_breaker.py"
      provides: "is_circuit_open(), record_api_failure(), record_api_success()"
      exports: ["is_circuit_open", "record_api_failure", "record_api_success"]
    - path: "backend/app/services/thresholds.py"
      provides: "get_threshold() con lookup DB + env var fallback"
      exports: ["get_threshold"]
    - path: "backend/app/tasks/mikrotik.py"
      provides: "poll_mikrotik_device() Celery task + _collect_mikrotik_async()"
      exports: ["poll_mikrotik_device"]
    - path: "backend/tests/unit/test_mikrotik.py"
      provides: "Tests MK-01, MK-02, MK-03"
    - path: "backend/tests/unit/test_circuit_breaker.py"
      provides: "Tests MK-04"
    - path: "backend/tests/unit/test_thresholds.py"
      provides: "Tests ALERT-06"
  key_links:
    - from: "backend/app/tasks/mikrotik.py"
      to: "backend/app/services/circuit_breaker.py"
      via: "is_circuit_open() llamado al inicio de _collect_mikrotik_async()"
      pattern: "is_circuit_open"
    - from: "backend/app/tasks/mikrotik.py"
      to: "backend/app/models/metric.py"
      via: "insert(Metric) con lista de dicts"
      pattern: "insert\\(Metric\\)"
    - from: "backend/app/services/thresholds.py"
      to: "backend/app/models/alert.py"
      via: "select(Alert.threshold_value).where(Alert.alert_type == alert_type)"
      pattern: "Alert\\.threshold_value"

user_setup:
  - service: mikrotik_routeros
    why: "Acceso RouterOS API para recolectar metricas"
    env_vars:
      - name: MIKROTIK_API_PORT
        source: "Puerto RouterOS API del equipo (8728 sin SSL, 8729 con SSL). Default: 8728"
    dashboard_config:
      - task: "Habilitar RouterOS API en cada Mikrotik"
        location: "Mikrotik: IP -> Services -> api (enable) o api-ssl (enable)"
---

<objective>
Collector Mikrotik + circuit breaker Redis + servicio de umbrales configurables.

Purpose: Permite que el worker recolecte CPU%, RAM% y trafico TX/RX por interfaz de
cada router Mikrotik via RouterOS API, con proteccion de circuit breaker para no
saturar equipos con fallos y umbrales configurables desde DB o variables de entorno.

Output:
- `backend/app/tasks/mikrotik.py`: Celery task `poll_mikrotik_device(device_id)` que
  recolecta metricas y las escribe en la tabla `metrics`.
- `backend/app/services/circuit_breaker.py`: Helpers Redis para circuit breaker por
  dispositivo (prefijo `cb:`, TTL 5 min).
- `backend/app/services/thresholds.py`: `get_threshold()` con lookup DB > env var.
- 3 archivos de tests cubriendo MK-01, MK-02, MK-03, MK-04, ALERT-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mikrotik-alertas-incidentes/03-RESEARCH.md

<interfaces>
<!-- Interfaces existentes que el executor necesita. Extraidas del codebase. -->

De backend/app/models/device.py:
```python
class DeviceType(str, enum.Enum):
    MIKROTIK = "mikrotik"

class Device(Base):
    __tablename__ = "devices"
    id: Mapped[int]
    name: Mapped[str]
    ip_address: Mapped[str]
    device_type: Mapped[DeviceType]
    site: Mapped[str | None]
    status: Mapped[DeviceStatus]
    is_active: Mapped[bool]
```

De backend/app/models/metric.py:
```python
class Metric(Base):
    __tablename__ = "metrics"
    id: Mapped[int]
    device_id: Mapped[int]          # FK devices.id
    metric_name: Mapped[str]        # "cpu_pct", "ram_pct", "rx_bps", "tx_bps"
    value: Mapped[Decimal]          # Numeric(12, 4)
    unit: Mapped[str | None]        # "%", "bps"
    interface: Mapped[str | None]   # nombre interfaz Mikrotik (ether1, wlan1)
    recorded_at: Mapped[datetime]   # DateTime(timezone=True)
```

De backend/app/models/alert.py:
```python
class Alert(Base):
    __tablename__ = "alerts"
    id: Mapped[int]
    device_id: Mapped[int | None]       # NULL = alerta global
    alert_type: Mapped[str]             # "cpu_high", "signal_low"
    threshold_value: Mapped[Decimal | None]
    is_active: Mapped[bool]
    consecutive_polls_required: Mapped[int]
```

De backend/app/core/config.py (Settings ya existentes):
```python
settings.REDIS_URL: str
settings.CPU_ALERT_THRESHOLD_PCT: float = 90.0
settings.ONU_SIGNAL_MIN_DBM: float = -28.0
settings.CONSECUTIVE_FAILURES_THRESHOLD: int = 3
# NUEVAS a agregar en Task 1:
# settings.MIKROTIK_API_PORT: int = 8728
# settings.ALERT_DEBOUNCE_SECONDS: int = 120
```

De backend/app/core/database.py:
```python
AsyncSessionLocal  # async context manager para sesiones SQLAlchemy
```

De backend/app/tasks/polling.py (patron establecido para tasks Celery):
```python
@shared_task(name="tasks.poll_all_devices")
def poll_all_devices() -> dict:
    return asyncio.run(_poll_all_devices_async())
```

De backend/app/celery_app.py:
```python
celery_app = Celery("beepyred_noc", ...)
celery_app.conf.update(
    include=["app.tasks.polling"],  # agregar "app.tasks.mikrotik" en Task 3
    beat_schedule={...}
)
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Tarea 1: Settings nuevas + circuit breaker Redis + servicio de umbrales</name>
  <files>
    backend/app/core/config.py,
    backend/app/services/__init__.py,
    backend/app/services/circuit_breaker.py,
    backend/app/services/thresholds.py,
    backend/tests/unit/test_circuit_breaker.py,
    backend/tests/unit/test_thresholds.py
  </files>
  <behavior>
    Circuit breaker:
    - Test: is_circuit_open() retorna False cuando no hay clave Redis cb:open:{id}
    - Test: record_api_failure() 3 veces abre el circuit (setex cb:open:{id} con TTL 300)
    - Test: is_circuit_open() retorna True despues de abrirse
    - Test: record_api_success() elimina cb:open:{id} y cb:fails:{id}
    - Test: circuit se auto-resetea (clave expira — verificar TTL via mock)

    Umbrales:
    - Test: get_threshold("cpu_high", device_id=5) retorna umbral especifico de DB si existe
    - Test: get_threshold("cpu_high", device_id=5) retorna umbral global si no hay especifico
    - Test: get_threshold("cpu_high") retorna settings.CPU_ALERT_THRESHOLD_PCT como fallback
    - Test: get_threshold("signal_low") retorna settings.ONU_SIGNAL_MIN_DBM como fallback
    - Test: get_threshold("tipo_inexistente") retorna None
  </behavior>
  <action>
    RED: Crear test_circuit_breaker.py y test_thresholds.py con los comportamientos
    listados. Usar unittest.mock.AsyncMock para el cliente Redis y AsyncMock para
    AsyncSession. Ejecutar — deben fallar (ModuleNotFoundError o AssertionError).

    GREEN: Implementar en este orden:

    1. backend/app/core/config.py — agregar al final de la clase Settings:
       ```python
       # -- Mikrotik RouterOS API (Phase 3) -----------------------------------------
       MIKROTIK_API_PORT: int = 8728

       # -- Alertas (Phase 3) --------------------------------------------------------
       # Debounce anti-flapping: segundos entre deteccion DOWN y envio de alerta.
       # Si el equipo se recupera en ese intervalo, no se envia alerta.
       ALERT_DEBOUNCE_SECONDS: int = 120
       ```

    2. backend/app/services/__init__.py — archivo vacio (marca el paquete).

    3. backend/app/services/circuit_breaker.py — implementar exactamente:
       ```python
       """
       Circuit breaker por dispositivo usando Redis TTL.

       MK-04: Suspende polling RouterOS API 5 minutos tras 3 fallos consecutivos.
       Separado del circuit breaker ICMP (consecutive_failures en DB).

       Claves Redis:
         cb:fails:{device_id}  — contador de fallos (expira en CIRCUIT_OPEN_TTL)
         cb:open:{device_id}   — indica circuit abierto (TTL = tiempo de suspension)
       """
       import redis.asyncio as aioredis

       CIRCUIT_OPEN_TTL: int = 5 * 60   # 5 minutos (MK-04)
       CIRCUIT_FAIL_THRESHOLD: int = 3  # 3 fallos RouterOS API consecutivos


       async def is_circuit_open(redis_client: aioredis.Redis, device_id: int) -> bool:
           """True si el circuit breaker esta abierto para este dispositivo."""
           return await redis_client.exists(f"cb:open:{device_id}") > 0


       async def record_api_failure(redis_client: aioredis.Redis, device_id: int) -> bool:
           """
           Incrementa contador de fallos RouterOS API.
           Si alcanza CIRCUIT_FAIL_THRESHOLD, abre el circuit (setex con TTL).
           Retorna True si el circuit acaba de abrirse.
           """
           fail_key = f"cb:fails:{device_id}"
           count = await redis_client.incr(fail_key)
           await redis_client.expire(fail_key, CIRCUIT_OPEN_TTL)

           if count >= CIRCUIT_FAIL_THRESHOLD:
               await redis_client.setex(f"cb:open:{device_id}", CIRCUIT_OPEN_TTL, "1")
               await redis_client.delete(fail_key)
               return True
           return False


       async def record_api_success(redis_client: aioredis.Redis, device_id: int) -> None:
           """Resetea el circuit breaker al conectar exitosamente."""
           await redis_client.delete(f"cb:fails:{device_id}")
           await redis_client.delete(f"cb:open:{device_id}")
       ```

    4. backend/app/services/thresholds.py — implementar exactamente:
       ```python
       """
       Servicio de umbrales de alerta configurables (MK-04, ALERT-05, ALERT-06).

       Orden de precedencia:
         1. Umbral especifico por device_id en tabla alerts
         2. Umbral global (device_id IS NULL) en tabla alerts
         3. Variable de entorno en settings (fallback)
       """
       from sqlalchemy import select
       from sqlalchemy.ext.asyncio import AsyncSession

       from app.core.config import settings
       from app.models.alert import Alert


       async def get_threshold(
           db: AsyncSession,
           alert_type: str,
           device_id: int | None = None,
       ) -> float | None:
           """
           Retorna el umbral activo para alert_type.
           device_id=None busca solo umbral global + env var fallback.
           """
           # 1. Umbral especifico por dispositivo
           if device_id is not None:
               result = await db.execute(
                   select(Alert.threshold_value)
                   .where(Alert.device_id == device_id)
                   .where(Alert.alert_type == alert_type)
                   .where(Alert.is_active.is_(True))
                   .limit(1)
               )
               row = result.scalar_one_or_none()
               if row is not None:
                   return float(row)

           # 2. Umbral global (device_id IS NULL)
           result = await db.execute(
               select(Alert.threshold_value)
               .where(Alert.device_id.is_(None))
               .where(Alert.alert_type == alert_type)
               .where(Alert.is_active.is_(True))
               .limit(1)
           )
           row = result.scalar_one_or_none()
           if row is not None:
               return float(row)

           # 3. Fallback a env var
           _defaults: dict[str, float] = {
               "cpu_high": settings.CPU_ALERT_THRESHOLD_PCT,
               "signal_low": settings.ONU_SIGNAL_MIN_DBM,
           }
           return _defaults.get(alert_type)
       ```

    REFACTOR: Verificar que los imports son correctos, ninguna variable sin usar.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_circuit_breaker.py tests/unit/test_thresholds.py -x -q</automated>
  </verify>
  <done>
    - tests/unit/test_circuit_breaker.py: todos los tests pasan (MK-04)
    - tests/unit/test_thresholds.py: todos los tests pasan (ALERT-06)
    - backend/app/core/config.py tiene MIKROTIK_API_PORT y ALERT_DEBOUNCE_SECONDS
    - backend/app/services/circuit_breaker.py exporta is_circuit_open, record_api_failure, record_api_success
    - backend/app/services/thresholds.py exporta get_threshold
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Tarea 2: librouteros en requirements + task Celery Mikrotik collector</name>
  <files>
    backend/requirements.txt,
    backend/app/tasks/mikrotik.py,
    backend/tests/unit/test_mikrotik.py
  </files>
  <behavior>
    - Test: poll_mikrotik_device() es un Celery task registrado con name="tasks.poll_mikrotik_device"
    - Test: cuando circuit breaker esta abierto, retorna {"skipped": True, "reason": "circuit_open"} sin llamar async_connect
    - Test: _collect_mikrotik_async() llama api.path('system', 'resource') y api.path('interface')
    - Test: cpu_pct se calcula correctamente desde el campo "cpu-load" (int, ya es porcentaje)
    - Test: ram_pct se calcula correctamente: (1 - free_memory/total_memory) * 100
    - Test: interfaces se parsean: por cada interfaz con "tx-bits-per-second" se genera metric tx_bps
    - Test: write_metrics() usa insert(Metric) con lista de dicts (una transaccion por dispositivo)
    - Test: api.close() se llama en bloque finally aunque falle la recoleccion
    - Test: si la conexion RouterOS API falla, llama record_api_failure() y relanza
    - Test: si la conexion RouterOS API tiene exito, llama record_api_success()
    - Test: campos RouterOS API ausentes se manejan con .get() — no lanza KeyError
  </behavior>
  <action>
    RED: Crear test_mikrotik.py con los comportamientos. Usar unittest.mock.patch
    para librouteros.async_connect, AsyncMock para el api object, MagicMock para
    api.path(). El api object debe soportar "async for item in path_obj" — usar
    AsyncMock con __aiter__. Ejecutar — deben fallar.

    GREEN:

    1. backend/requirements.txt — agregar las dos nuevas dependencias al final de
       la seccion de librerias de dispositivos (antes de la linea de httpx si existe,
       o al final del bloque de dependencias de device integration):
       ```
       librouteros==4.0.1
       python-telegram-bot==21.*
       ```
       NOTA: python-telegram-bot se agrega aqui para que el install del Plan 02 lo
       incluya; se usa en el Plan 02 (alertas).

    2. backend/app/tasks/mikrotik.py — implementar:
       ```python
       """
       Collector RouterOS API para routers Mikrotik.

       MK-01: CPU% y RAM% via /system/resource
       MK-02: TX/RX bps por interfaz via /interface
       MK-03: api.close() en finally — sin sesiones zombie
       MK-04: circuit breaker Redis — is_circuit_open() ANTES de conectar
       """
       import asyncio
       import logging
       from datetime import datetime, timezone

       import redis.asyncio as aioredis
       from celery import shared_task
       from librouteros import async_connect
       from sqlalchemy import insert

       from app.core.config import settings
       from app.core.database import AsyncSessionLocal
       from app.models.device import Device, DeviceType
       from app.models.metric import Metric
       from app.services.circuit_breaker import (
           is_circuit_open,
           record_api_failure,
           record_api_success,
       )

       logger = logging.getLogger(__name__)


       @shared_task(name="tasks.poll_mikrotik_device")
       def poll_mikrotik_device(device_id: int) -> dict:
           """
           MK-01/02: Recolecta metricas CPU, RAM e interfaces de un Mikrotik.
           Llamado por poll_all_devices() para dispositivos de tipo MIKROTIK.
           """
           return asyncio.run(_collect_mikrotik_async(device_id))


       async def _collect_mikrotik_async(device_id: int) -> dict:
           """Logica async interna del collector Mikrotik."""
           redis_client = aioredis.from_url(settings.REDIS_URL, decode_responses=True)
           try:
               # MK-04: verificar circuit breaker ANTES de conectar
               if await is_circuit_open(redis_client, device_id):
                   logger.info("device %s: circuit open, skipping RouterOS poll", device_id)
                   return {"skipped": True, "reason": "circuit_open", "device_id": device_id}

               # Obtener credenciales del dispositivo
               async with AsyncSessionLocal() as db:
                   from sqlalchemy import select
                   result = await db.execute(
                       select(Device).where(Device.id == device_id)
                   )
                   device = result.scalar_one_or_none()
                   if device is None or device.device_type != DeviceType.MIKROTIK:
                       return {"skipped": True, "reason": "not_mikrotik", "device_id": device_id}

                   # Obtener credencial RouterOS API
                   from app.models.device_credential import DeviceCredential
                   from app.core.crypto import decrypt_credential
                   cred_result = await db.execute(
                       select(DeviceCredential)
                       .where(DeviceCredential.device_id == device_id)
                       .where(DeviceCredential.credential_type == "routeros_api")
                       .limit(1)
                   )
                   cred = cred_result.scalar_one_or_none()
                   if cred is None:
                       logger.warning("device %s: no routeros_api credential found", device_id)
                       return {"skipped": True, "reason": "no_credential", "device_id": device_id}

                   username = cred.username
                   password = decrypt_credential(cred.encrypted_password)
                   ip = device.ip_address

               # Conectar y recolectar
               raw = await _fetch_routeros_data(ip, username, password, device_id, redis_client)
               if raw is None:
                   return {"collected": False, "device_id": device_id}

               metrics = _parse_metrics(raw)
               await _write_metrics(device_id, metrics)
               await record_api_success(redis_client, device_id)

               return {"collected": True, "device_id": device_id, "metrics_count": len(metrics)}

           finally:
               await redis_client.aclose()


       async def _fetch_routeros_data(
           ip: str,
           username: str,
           password: str,
           device_id: int,
           redis_client: aioredis.Redis,
       ) -> dict | None:
           """
           Abre conexion RouterOS API, recolecta resource + interfaces, cierra.
           MK-03: api.close() siempre en finally.
           """
           api = None
           try:
               api = await async_connect(
                   username=username,
                   password=password,
                   host=ip,
                   port=settings.MIKROTIK_API_PORT,
               )
               # /system/resource — CPU y RAM
               resource_path = api.path("system", "resource")
               resources = [item async for item in resource_path]
               resource = resources[0] if resources else {}

               # /interface — trafico por interfaz
               iface_path = api.path("interface")
               interfaces = [item async for item in iface_path]

               return {"resource": resource, "interfaces": interfaces}

           except Exception as exc:
               logger.error("device %s: RouterOS API error: %s", device_id, exc)
               just_opened = await record_api_failure(redis_client, device_id)
               if just_opened:
                   logger.warning(
                       "device %s: circuit breaker opened (3 consecutive API failures)",
                       device_id,
                   )
               return None
           finally:
               if api is not None:
                   try:
                       api.close()  # MK-03 — cierre explicito, siempre
                   except Exception:
                       pass  # no propagar errores de cierre


       def _parse_metrics(raw: dict) -> list[dict]:
           """
           Convierte la respuesta RouterOS API a lista de dicts para insert(Metric).

           RouterOS 6.x y 7.x usan los mismos nombres de campo en /system/resource.
           Usa .get() defensivo — campos ausentes resultan en metrica omitida (no error).
           """
           metrics: list[dict] = []
           resource = raw.get("resource", {})
           now = datetime.now(timezone.utc)

           # MK-01: CPU% — campo "cpu-load" ya es porcentaje (int 0-100)
           cpu_load = resource.get("cpu-load")
           if cpu_load is not None:
               metrics.append({
                   "metric_name": "cpu_pct",
                   "value": float(cpu_load),
                   "unit": "%",
                   "interface": None,
                   "recorded_at": now,
               })

           # MK-01: RAM% — calcular desde free-memory y total-memory (bytes)
           free_mem = resource.get("free-memory")
           total_mem = resource.get("total-memory")
           if free_mem is not None and total_mem is not None and int(total_mem) > 0:
               ram_pct = (1.0 - int(free_mem) / int(total_mem)) * 100.0
               metrics.append({
                   "metric_name": "ram_pct",
                   "value": round(ram_pct, 2),
                   "unit": "%",
                   "interface": None,
                   "recorded_at": now,
               })

           # MK-02: TX/RX bps por interfaz
           for iface in raw.get("interfaces", []):
               iface_name = iface.get("name")
               if not iface_name:
                   continue
               tx_bps = iface.get("tx-bits-per-second")
               rx_bps = iface.get("rx-bits-per-second")
               if tx_bps is not None:
                   metrics.append({
                       "metric_name": "tx_bps",
                       "value": float(tx_bps),
                       "unit": "bps",
                       "interface": iface_name,
                       "recorded_at": now,
                   })
               if rx_bps is not None:
                   metrics.append({
                       "metric_name": "rx_bps",
                       "value": float(rx_bps),
                       "unit": "bps",
                       "interface": iface_name,
                       "recorded_at": now,
                   })

           return metrics


       async def _write_metrics(device_id: int, metrics: list[dict]) -> None:
           """
           MK-03: Inserta todas las metricas de un ciclo en una sola transaccion.
           Usa insert(Metric) con lista — SQLAlchemy 2.0 usa executemany via asyncpg.
           """
           if not metrics:
               return
           rows = [{"device_id": device_id, **m} for m in metrics]
           async with AsyncSessionLocal() as db:
               await db.execute(insert(Metric), rows)
               await db.commit()
       ```

    REFACTOR: Asegurar que los imports esten en orden (stdlib, third-party, local).
    Verificar que _parse_metrics no tiene side effects — es funcion pura testeable.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_mikrotik.py -x -q</automated>
  </verify>
  <done>
    - tests/unit/test_mikrotik.py: todos los tests pasan (MK-01, MK-02, MK-03, MK-04)
    - backend/requirements.txt incluye librouteros==4.0.1 y python-telegram-bot==21.*
    - backend/app/tasks/mikrotik.py exporta poll_mikrotik_device
    - circuit breaker verificado ANTES de async_connect (test verifica el orden)
    - api.close() en finally (test verifica que se llama aunque falle la recoleccion)
  </done>
</task>

<task type="auto">
  <name>Tarea 3: Registrar task Mikrotik en celery_app + suite completa verde</name>
  <files>
    backend/app/celery_app.py
  </files>
  <action>
    Actualizar backend/app/celery_app.py para incluir el nuevo modulo de tasks
    y agregar el beat schedule para el collector Mikrotik:

    1. Cambiar la lista `include` de:
       ```python
       include=["app.tasks.polling"],
       ```
       a:
       ```python
       include=[
           "app.tasks.polling",
           "app.tasks.mikrotik",
           # app.tasks.alerts    — se agrega en Plan 02
           # app.tasks.maintenance — se agrega en Plan 03
       ],
       ```

    2. Agregar entrada al beat_schedule dentro de celery_app.conf.update:
       ```python
       "poll-mikrotik-devices": {
           "task": "tasks.poll_all_mikrotik",
           "schedule": settings.POLL_INTERVAL_SECONDS,
           "options": {"expires": settings.POLL_INTERVAL_SECONDS - 5},
       },
       ```

       NOTA: `tasks.poll_all_mikrotik` es una tarea que orquesta el polling de todos
       los Mikrotik activos. Agregarla al final de mikrotik.py:
       ```python
       @shared_task(name="tasks.poll_all_mikrotik")
       def poll_all_mikrotik() -> dict:
           """
           Orquesta el polling de todos los dispositivos Mikrotik activos.
           Encola un poll_mikrotik_device por cada Mikrotik en inventario.
           """
           return asyncio.run(_poll_all_mikrotik_async())


       async def _poll_all_mikrotik_async() -> dict:
           async with AsyncSessionLocal() as db:
               from sqlalchemy import select
               result = await db.execute(
                   select(Device)
                   .where(Device.is_active.is_(True))
                   .where(Device.device_type == DeviceType.MIKROTIK)
               )
               devices = list(result.scalars().all())

           if not devices:
               return {"queued": 0}

           for device in devices:
               poll_mikrotik_device.delay(device.id)

           return {"queued": len(devices)}
       ```

    3. Ejecutar la suite completa de unit tests para confirmar que no hay regresiones:
       `cd backend && python -m pytest tests/unit/ -x -q`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/ -x -q</automated>
  </verify>
  <done>
    - backend/app/celery_app.py incluye "app.tasks.mikrotik" en include
    - beat_schedule tiene entrada "poll-mikrotik-devices"
    - Suite completa tests/unit/ pasa sin regresiones (minimo 51 tests previos + nuevos)
    - poll_all_mikrotik task registrado en mikrotik.py
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker Celery -> RouterOS API | Credenciales descifradas en runtime para conectar al Mikrotik; datos de red de dispositivo privado entran al worker |
| RouterOS API response -> DB | Datos no estructurados del dispositivo se parsean antes de insertar en metrics |
| Settings -> MIKROTIK_API_PORT | Puerto configurable que determina a que endpoint se conecta el worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | `_fetch_routeros_data` — username/password en memoria | mitigate | No loggear username ni password. Solo loggear device_id y tipo de error. `decrypt_credential()` solo en runtime, result no se persiste. |
| T-3-02 | Tampering | `_parse_metrics` — valores RouterOS inyectados en DB | mitigate | Usar `.get()` defensivo con cast explicito a `float()`. Numeric(12,4) en columna `value` limita magnitud. Campos string (interface name) son solo descriptivos, no ejecutables. |
| T-3-03 | Denial of Service | Circuit breaker — sin limite maximo de keys Redis | accept | Con max 500 dispositivos Mikrotik: max 1000 keys Redis (cb:open + cb:fails). Negligible. TTL 5min auto-expira. |
| T-3-04 | Elevation of Privilege | RouterOS API port configurable via env var | accept | MIKROTIK_API_PORT es entero validado por Pydantic (int). Solo cambia el puerto de destino. No afecta el servidor NOC. |
| T-3-05 | Information Disclosure | Redis keys `cb:open:{id}` y `cb:fails:{id}` | accept | Redis es interno (Railway private network). No contienen datos sensibles — solo contadores y flags. |
| T-3-06 | Spoofing | RouterOS API — no verificacion de certificado SSL en modo api (8728) | accept | Puerto 8728 no usa TLS — mismo que la red privada ISP usa hoy. Para v2: migrar a port 8729 con SSL. Aceptado para v1. |
</threat_model>

<verification>
Verificacion completa del Plan 01:

```bash
# Desde la raiz del proyecto:

# 1. Confirmar nuevas dependencias en requirements.txt
grep -E "librouteros|python-telegram-bot" backend/requirements.txt

# 2. Confirmar nuevas settings en config.py
grep -E "MIKROTIK_API_PORT|ALERT_DEBOUNCE_SECONDS" backend/app/core/config.py

# 3. Confirmar archivos creados
ls backend/app/services/circuit_breaker.py
ls backend/app/services/thresholds.py
ls backend/app/tasks/mikrotik.py

# 4. Confirmar que circuit breaker se verifica ANTES de async_connect
grep -n "is_circuit_open" backend/app/tasks/mikrotik.py | head -5

# 5. Confirmar api.close() en bloque finally
grep -n "api.close\|finally" backend/app/tasks/mikrotik.py

# 6. Suite de tests
cd backend && python -m pytest tests/unit/ -v --tb=short
```
</verification>

<success_criteria>
- [ ] `librouteros==4.0.1` y `python-telegram-bot==21.*` en backend/requirements.txt
- [ ] `settings.MIKROTIK_API_PORT` y `settings.ALERT_DEBOUNCE_SECONDS` en config.py
- [ ] `is_circuit_open()` retorna False cuando no hay clave Redis
- [ ] `record_api_failure()` abre circuit tras 3 fallos (setex cb:open:{id} TTL 300)
- [ ] `record_api_success()` elimina ambas claves cb:
- [ ] `get_threshold()` prioriza DB device-especifico > global > env var
- [ ] `poll_mikrotik_device()` verifica circuit breaker ANTES de llamar async_connect
- [ ] `api.close()` se llama en bloque finally (MK-03)
- [ ] `_parse_metrics()` maneja campos ausentes con .get() (sin KeyError)
- [ ] `_write_metrics()` usa insert(Metric) con lista — una transaccion por dispositivo
- [ ] `poll_all_mikrotik` registrado en celery_app.conf beat_schedule
- [ ] Suite completa `tests/unit/` pasa sin regresiones
</success_criteria>

<output>
Tras completar todas las tareas, crear:
`.planning/phases/03-mikrotik-alertas-incidentes/03-01-SUMMARY.md`

El SUMMARY debe incluir:
- Frontmatter YAML con: phase, plan, subsystem, tags, dependency_graph, tech_stack (librouteros==4.0.1 agregado), key_files (created/modified), decisions, metrics
- Seccion "What Was Built" describiendo circuit_breaker.py, thresholds.py, mikrotik.py
- Seccion "Interfaces Exported" con las firmas exactas de funciones publicas
- Seccion "Test Results" con conteo de tests pasando
- Seccion "Deviations from Plan" (si aplica)
- Seccion "Known Stubs" — documentar si tx-bits-per-second no esta disponible y se uso alternativa
- Seccion "Threat Flags" — estado de T-3-01 a T-3-06
</output>
