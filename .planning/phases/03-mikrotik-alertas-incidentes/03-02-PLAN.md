---
phase: 03-mikrotik-alertas-incidentes
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/services/telegram.py
  - backend/app/tasks/alerts.py
  - backend/app/tasks/polling.py
  - backend/app/celery_app.py
  - backend/tests/unit/test_alerts.py
  - backend/tests/unit/test_telegram.py
autonomous: true
requirements:
  - ALERT-01
  - ALERT-02
  - ALERT-03
  - ALERT-04
  - ALERT-05
  - INC-01
  - INC-02

must_haves:
  truths:
    - "Cuando _ping_and_update detecta DOWN, encola handle_device_down con countdown=ALERT_DEBOUNCE_SECONDS"
    - "handle_device_down abre un incidente en DB si no hay uno abierto (SELECT FOR UPDATE)"
    - "Si el incidente ya esta cerrado cuando se ejecuta handle_device_down, no envia Telegram"
    - "handle_device_recovery cierra el incidente con duration_seconds calculado y envia Telegram UP"
    - "El mensaje DOWN contiene nombre, IP, sitio y timestamp"
    - "El mensaje UP contiene nombre, IP y duracion formateada (Xh Xm Xs)"
    - "alert_sent=True se marca en el incidente despues de enviar la alerta DOWN"
    - "recovery_alert_sent=True se marca en el incidente despues de enviar la alerta UP"
  artifacts:
    - path: "backend/app/services/telegram.py"
      provides: "send_telegram_alert(), format_down_message(), format_up_message()"
      exports: ["send_telegram_alert", "format_down_message", "format_up_message"]
    - path: "backend/app/tasks/alerts.py"
      provides: "handle_device_down(), handle_device_recovery(), open_incident_if_not_exists(), close_incident()"
      exports: ["handle_device_down", "handle_device_recovery"]
    - path: "backend/tests/unit/test_alerts.py"
      provides: "Tests ALERT-01, ALERT-04, ALERT-05, INC-01, INC-02"
    - path: "backend/tests/unit/test_telegram.py"
      provides: "Tests ALERT-02, ALERT-03"
  key_links:
    - from: "backend/app/tasks/polling.py"
      to: "backend/app/tasks/alerts.py"
      via: "handle_device_down.apply_async(args=[device_id], countdown=ALERT_DEBOUNCE_SECONDS)"
      pattern: "handle_device_down\\.apply_async"
    - from: "backend/app/tasks/alerts.py"
      to: "backend/app/models/incident.py"
      via: "open_incident_if_not_exists() con SELECT FOR UPDATE"
      pattern: "with_for_update"
    - from: "backend/app/tasks/alerts.py"
      to: "backend/app/services/telegram.py"
      via: "send_telegram_alert() llamado tras abrir/cerrar incidente"
      pattern: "send_telegram_alert"
---

<objective>
Pipeline completo de alertas Telegram + ciclo de vida de incidentes.

Purpose: Cuando el worker ICMP detecta que un equipo pasa a DOWN o se recupera,
dispara tasks Celery que abren/cierran incidentes en DB de forma atomica y envian
mensajes Telegram con contexto completo del evento.

Output:
- `backend/app/services/telegram.py`: wrapper async para Bot outbound-only + formateadores de mensajes.
- `backend/app/tasks/alerts.py`: handle_device_down y handle_device_recovery con logica de incidente atomica.
- `backend/app/tasks/polling.py` modificado: dispara alerts tasks al detectar transicion.
- 2 archivos de tests cubriendo ALERT-01 a ALERT-05, INC-01, INC-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mikrotik-alertas-incidentes/03-RESEARCH.md

<interfaces>
<!-- Interfaces existentes que el executor necesita. Extraidas del codebase. -->

De backend/app/models/incident.py:
```python
class Incident(Base):
    __tablename__ = "incidents"
    id: Mapped[int]
    device_id: Mapped[int]             # FK devices.id CASCADE
    started_at: Mapped[datetime]       # DateTime(timezone=True), NOT NULL
    resolved_at: Mapped[datetime | None]    # NULL = incidente abierto
    duration_seconds: Mapped[int | None]    # calculado al cerrar
    cause: Mapped[str | None]
    alert_sent: Mapped[bool]           # default=False
    recovery_alert_sent: Mapped[bool]  # default=False
```

De backend/app/models/device.py:
```python
class Device(Base):
    id: Mapped[int]
    name: Mapped[str]
    ip_address: Mapped[str]
    site: Mapped[str | None]
    status: Mapped[DeviceStatus]       # UP, DOWN, WARNING, UNKNOWN

class DeviceStatus(str, enum.Enum):
    UP = "up"
    DOWN = "down"
```

De backend/app/core/config.py:
```python
settings.TELEGRAM_BOT_TOKEN: str = ""   # SecretStr en Phase 3 NO — es str simple
settings.TELEGRAM_CHAT_ID: str = ""
settings.ALERT_DEBOUNCE_SECONDS: int = 120  # agregado en Plan 01
```

De backend/app/core/database.py:
```python
AsyncSessionLocal  # async context manager
```

De backend/app/tasks/polling.py (a modificar — solo _ping_and_update):
```python
async def _ping_and_update(device: Device) -> bool:
    # ... logica existente de ping + debounce ...
    # MODIFICAR: al detectar transicion UP->DOWN, agregar:
    #   handle_device_down.apply_async(args=[device.id], countdown=settings.ALERT_DEBOUNCE_SECONDS)
    # MODIFICAR: al detectar transicion DOWN->UP, agregar:
    #   handle_device_recovery.delay(device.id)
    # El import se agrega al inicio del modulo.
    # CRITICO: no romper los 11 tests existentes de test_polling.py
```

De backend/app/celery_app.py:
```python
include=["app.tasks.polling", "app.tasks.mikrotik"]
# MODIFICAR: agregar "app.tasks.alerts"
```

Patron Celery establecido (polling.py):
```python
@shared_task(name="tasks.handle_device_down")
def handle_device_down(device_id: int) -> dict:
    return asyncio.run(_handle_device_down_async(device_id))
```

python-telegram-bot v21 — patron outbound-only verificado en RESEARCH.md:
```python
from telegram import Bot
async with Bot(token=token) as bot:
    await bot.send_message(chat_id=chat_id, text=text, parse_mode="HTML")
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Tarea 1: Servicio Telegram + formateadores de mensajes</name>
  <files>
    backend/app/services/telegram.py,
    backend/tests/unit/test_telegram.py
  </files>
  <behavior>
    - Test: format_down_message() incluye el nombre del equipo en el resultado
    - Test: format_down_message() incluye la IP en etiqueta `code` HTML
    - Test: format_down_message() incluye el sitio o "Sin sitio" si es None
    - Test: format_down_message() incluye el timestamp formateado como dd/mm/yyyy HH:MM:SS UTC
    - Test: format_up_message() incluye "EQUIPO RECUPERADO" en el resultado
    - Test: format_up_message() formatea duracion 3661 segundos como "1h 1m 1s"
    - Test: format_up_message() formatea duracion 90 segundos como "1m 30s" (sin horas)
    - Test: send_telegram_alert() no hace nada si TELEGRAM_BOT_TOKEN esta vacio
    - Test: send_telegram_alert() llama bot.send_message() con parse_mode="HTML"
    - Test: send_telegram_alert() usa async context manager (async with Bot(...))
  </behavior>
  <action>
    RED: Crear test_telegram.py con los comportamientos. Usar unittest.mock.patch
    para `telegram.Bot` — mockear el context manager async (`__aenter__`/`__aexit__`).
    Para send_telegram_alert con token vacio: verificar que no se llama Bot().
    Ejecutar — deben fallar.

    GREEN: Crear backend/app/services/telegram.py:
    ```python
    """
    Servicio de alertas Telegram para BEEPYRED NOC.

    ALERT-02: Envia mensaje DOWN con nombre, IP, sitio, timestamp.
    ALERT-03: Envia mensaje UP con duracion de la caida.

    Patron outbound-only — sin webhook. El bot envia mensajes pero no recibe.
    Usar `async with Bot(token) as bot:` — inicializa/cierra la sesion HTTP correctamente.
    Parse mode HTML: mas predecible que Markdown para mensajes programaticos.
    """
    import logging
    from datetime import datetime

    from app.core.config import settings

    logger = logging.getLogger(__name__)


    def format_down_message(
        device_name: str,
        ip: str,
        site: str | None,
        timestamp: datetime,
    ) -> str:
        """
        ALERT-02: Formato HTML para mensaje de equipo caido.
        Incluye nombre, IP, sitio y timestamp UTC.
        """
        ts = timestamp.strftime("%d/%m/%Y %H:%M:%S")
        site_str = site or "Sin sitio"
        return (
            f"🔴 <b>EQUIPO CAIDO</b>\n"
            f"<b>Nombre:</b> {device_name}\n"
            f"<b>IP:</b> <code>{ip}</code>\n"
            f"<b>Sitio:</b> {site_str}\n"
            f"<b>Hora:</b> {ts} UTC"
        )


    def format_up_message(
        device_name: str,
        ip: str,
        site: str | None,
        duration_seconds: int,
    ) -> str:
        """
        ALERT-03: Formato HTML para mensaje de equipo recuperado.
        Incluye nombre, IP, sitio y duracion de la caida formateada.
        """
        mins, secs = divmod(duration_seconds, 60)
        hours, mins = divmod(mins, 60)
        if hours:
            duration_str = f"{hours}h {mins}m {secs}s"
        else:
            duration_str = f"{mins}m {secs}s"

        site_str = site or "Sin sitio"
        return (
            f"🟢 <b>EQUIPO RECUPERADO</b>\n"
            f"<b>Nombre:</b> {device_name}\n"
            f"<b>IP:</b> <code>{ip}</code>\n"
            f"<b>Sitio:</b> {site_str}\n"
            f"<b>Duracion caida:</b> {duration_str}"
        )


    async def send_telegram_alert(text: str) -> None:
        """
        Envia un mensaje al chat configurado en TELEGRAM_CHAT_ID.

        Guard: si TELEGRAM_BOT_TOKEN o TELEGRAM_CHAT_ID estan vacios,
        loggea advertencia y retorna sin error — no bloquea el ciclo de alertas.

        Usa `async with Bot(token) as bot:` para inicializar la sesion HTTP
        interna de python-telegram-bot v21 correctamente.
        """
        token = settings.TELEGRAM_BOT_TOKEN
        chat_id = settings.TELEGRAM_CHAT_ID

        if not token or not chat_id:
            logger.warning(
                "Telegram alert suppressed: TELEGRAM_BOT_TOKEN or TELEGRAM_CHAT_ID not configured"
            )
            return

        from telegram import Bot  # import diferido — no falla si la lib no esta instalada aun
        async with Bot(token=token) as bot:
            await bot.send_message(
                chat_id=chat_id,
                text=text,
                parse_mode="HTML",
            )
    ```

    REFACTOR: Los formateadores son funciones puras — no tienen dependencias de settings.
    send_telegram_alert() usa import diferido para facilitar tests con mock.patch.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_telegram.py -x -q</automated>
  </verify>
  <done>
    - tests/unit/test_telegram.py: todos los tests pasan (ALERT-02, ALERT-03)
    - format_down_message() incluye nombre, IP en code HTML, sitio, timestamp UTC
    - format_up_message() formatea duracion correctamente (con y sin horas)
    - send_telegram_alert() retorna sin error si token/chat_id estan vacios
    - send_telegram_alert() usa async with Bot(...) con parse_mode="HTML"
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Tarea 2: Tasks handle_device_down y handle_device_recovery (ciclo de vida incidente)</name>
  <files>
    backend/app/tasks/alerts.py,
    backend/tests/unit/test_alerts.py
  </files>
  <behavior>
    handle_device_down:
    - Test: si el incidente ya esta cerrado (resolved_at != None) cuando se ejecuta, retorna sin enviar Telegram (debounce ALERT-04)
    - Test: si no hay incidente abierto, crea uno con started_at = ahora (INC-01)
    - Test: si ya hay un incidente abierto, lo reutiliza (no crea duplicado)
    - Test: marca alert_sent=True en el incidente despues de enviar Telegram (ALERT-05)
    - Test: llama send_telegram_alert con mensaje que contiene el nombre del equipo (ALERT-02)

    handle_device_recovery:
    - Test: si no hay incidente abierto, retorna sin error (caso edge: recovery sin DOWN previo)
    - Test: cierra el incidente con resolved_at y calcula duration_seconds (INC-02)
    - Test: marca recovery_alert_sent=True en el incidente
    - Test: llama send_telegram_alert con mensaje que incluye la duracion (ALERT-03)

    open_incident_if_not_exists:
    - Test: usa SELECT FOR UPDATE para prevenir race conditions (ALERT-05)
    - Test: retorna incidente existente si ya hay uno abierto (no inserta duplicado)

    close_incident:
    - Test: calcula duration_seconds = (resolved_at - started_at).total_seconds()
    - Test: retorna None si no hay incidente abierto
  </behavior>
  <action>
    RED: Crear test_alerts.py con los comportamientos. Usar unittest.mock.patch para
    `app.tasks.alerts.send_telegram_alert` (coroutine mock con AsyncMock). Mockear
    AsyncSessionLocal con un context manager que retorna una sesion mock.
    Mockear Device y Incident con objetos simples (dataclasses o MagicMock con atributos).
    Ejecutar — deben fallar.

    GREEN: Crear backend/app/tasks/alerts.py:
    ```python
    """
    Tasks Celery para pipeline de alertas y ciclo de vida de incidentes.

    ALERT-01: handle_device_down encola con countdown (debounce anti-flapping)
    ALERT-04: al ejecutarse, verifica si el incidente sigue abierto
    INC-01:   open_incident_if_not_exists — INSERT con SELECT FOR UPDATE
    INC-02:   close_incident — UPDATE resolved_at + duration_seconds
    ALERT-02: formato DOWN con nombre, IP, sitio, timestamp
    ALERT-03: formato UP con duracion de la caida
    ALERT-05: alert_sent / recovery_alert_sent en la fila de incidente
    """
    import asyncio
    import logging
    from datetime import datetime, timezone

    from celery import shared_task
    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.core.config import settings
    from app.core.database import AsyncSessionLocal
    from app.models.device import Device
    from app.models.incident import Incident
    from app.services.telegram import (
        format_down_message,
        format_up_message,
        send_telegram_alert,
    )

    logger = logging.getLogger(__name__)


    # ---------------------------------------------------------------------------
    # Celery tasks (sincronicos — mismo patron que polling.py)
    # ---------------------------------------------------------------------------

    @shared_task(name="tasks.handle_device_down")
    def handle_device_down(device_id: int) -> dict:
        """
        ALERT-01/04: Ejecutado con countdown=ALERT_DEBOUNCE_SECONDS desde polling.py.
        Al ejecutarse, verifica si el incidente sigue abierto (anti-flapping).
        """
        return asyncio.run(_handle_device_down_async(device_id))


    @shared_task(name="tasks.handle_device_recovery")
    def handle_device_recovery(device_id: int) -> dict:
        """
        ALERT-03/INC-02: Ejecutado inmediatamente al detectar DOWN->UP.
        Cierra incidente y envia mensaje Telegram con duracion.
        """
        return asyncio.run(_handle_device_recovery_async(device_id))


    # ---------------------------------------------------------------------------
    # Logica async interna
    # ---------------------------------------------------------------------------

    async def _handle_device_down_async(device_id: int) -> dict:
        """
        Abre incidente en DB y envia alerta Telegram DOWN.

        Debounce ALERT-04: el task se encoló con countdown=N segundos.
        Si el equipo se recupero antes del countdown, cuando este task
        se ejecute, el incidente ya estara cerrado (resolved_at != None) —
        en ese caso no se envia la alerta.
        """
        async with AsyncSessionLocal() as db:
            # Obtener dispositivo
            result = await db.execute(
                select(Device).where(Device.id == device_id)
            )
            device = result.scalar_one_or_none()
            if device is None:
                return {"skipped": True, "reason": "device_not_found"}

            # Abrir incidente atomicamente (SELECT FOR UPDATE)
            incident = await open_incident_if_not_exists(db, device_id)

            # ALERT-04 Debounce: si el incidente ya fue cerrado (equipo recupero antes
            # del countdown), no enviar la alerta DOWN
            if incident.resolved_at is not None:
                logger.info(
                    "device %s: recovered before debounce window, suppressing DOWN alert",
                    device_id,
                )
                return {"skipped": True, "reason": "recovered_before_debounce"}

            # No enviar doble alerta si ya fue enviada
            if incident.alert_sent:
                return {"skipped": True, "reason": "alert_already_sent"}

            # Formatear y enviar mensaje Telegram DOWN
            text = format_down_message(
                device_name=device.name,
                ip=device.ip_address,
                site=device.site,
                timestamp=incident.started_at,
            )
            await send_telegram_alert(text)

            # Marcar alerta enviada (ALERT-05)
            incident.alert_sent = True
            await db.commit()

        logger.info("device %s: DOWN alert sent, incident opened", device_id)
        return {"alerted": True, "device_id": device_id, "incident_id": incident.id}


    async def _handle_device_recovery_async(device_id: int) -> dict:
        """
        Cierra incidente abierto y envia alerta Telegram UP con duracion.
        """
        async with AsyncSessionLocal() as db:
            # Obtener dispositivo
            result = await db.execute(
                select(Device).where(Device.id == device_id)
            )
            device = result.scalar_one_or_none()
            if device is None:
                return {"skipped": True, "reason": "device_not_found"}

            # Cerrar incidente atomicamente
            incident = await close_incident(db, device_id)
            if incident is None:
                # No habia incidente abierto — recovery sin DOWN registrado
                logger.info(
                    "device %s: recovery without open incident, skipping", device_id
                )
                return {"skipped": True, "reason": "no_open_incident"}

            duration = incident.duration_seconds or 0

            # No enviar doble alerta de recovery
            if incident.recovery_alert_sent:
                return {"skipped": True, "reason": "recovery_alert_already_sent"}

            # Formatear y enviar mensaje Telegram UP
            text = format_up_message(
                device_name=device.name,
                ip=device.ip_address,
                site=device.site,
                duration_seconds=duration,
            )
            await send_telegram_alert(text)

            # Marcar recovery alert enviada (ALERT-05)
            incident.recovery_alert_sent = True
            await db.commit()

        logger.info(
            "device %s: UP alert sent, incident closed (duration: %ss)",
            device_id, duration,
        )
        return {"alerted": True, "device_id": device_id, "duration_seconds": duration}


    # ---------------------------------------------------------------------------
    # Helpers de incidentes (INC-01, INC-02)
    # ---------------------------------------------------------------------------

    async def open_incident_if_not_exists(
        db: AsyncSession,
        device_id: int,
    ) -> Incident:
        """
        INC-01: Abre un incidente si no hay uno abierto para el dispositivo.
        Usa SELECT FOR UPDATE para evitar race conditions entre workers Celery.
        Retorna el incidente abierto (nuevo o existente).
        """
        result = await db.execute(
            select(Incident)
            .where(Incident.device_id == device_id)
            .where(Incident.resolved_at.is_(None))
            .order_by(Incident.started_at.desc())
            .limit(1)
            .with_for_update()
        )
        existing = result.scalar_one_or_none()
        if existing is not None:
            return existing

        new_incident = Incident(
            device_id=device_id,
            started_at=datetime.now(timezone.utc),
            alert_sent=False,
            recovery_alert_sent=False,
        )
        db.add(new_incident)
        await db.flush()  # obtener ID asignado antes del commit
        return new_incident


    async def close_incident(
        db: AsyncSession,
        device_id: int,
    ) -> Incident | None:
        """
        INC-02: Cierra el incidente abierto mas reciente del dispositivo.
        Calcula duration_seconds = resolved_at - started_at.
        Usa SELECT FOR UPDATE para evitar doble cierre concurrente.
        Retorna None si no habia incidente abierto.
        """
        result = await db.execute(
            select(Incident)
            .where(Incident.device_id == device_id)
            .where(Incident.resolved_at.is_(None))
            .order_by(Incident.started_at.desc())
            .limit(1)
            .with_for_update()
        )
        incident = result.scalar_one_or_none()
        if incident is None:
            return None

        now = datetime.now(timezone.utc)
        incident.resolved_at = now
        incident.duration_seconds = int((now - incident.started_at).total_seconds())
        # No hacer commit aqui — el caller hace commit despues de marcar recovery_alert_sent
        return incident
    ```

    REFACTOR: Verificar que los paths de retorno son completos (ninguno retorna None
    sin documentarlo). Revisar que el indice parcial `idx_incidents_active` en DB
    cubra el WHERE de las queries (confirmed en 001_initial_schema.py).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_alerts.py -x -q</automated>
  </verify>
  <done>
    - tests/unit/test_alerts.py: todos los tests pasan (ALERT-01, ALERT-04, ALERT-05, INC-01, INC-02)
    - handle_device_down suprime alerta si incidente ya fue cerrado (debounce)
    - open_incident_if_not_exists usa with_for_update()
    - close_incident calcula duration_seconds correctamente
    - alert_sent y recovery_alert_sent marcados True despues de enviar cada alerta
  </done>
</task>

<task type="auto">
  <name>Tarea 3: Enganchar alertas en polling.py + registrar en celery_app</name>
  <files>
    backend/app/tasks/polling.py,
    backend/app/celery_app.py
  </files>
  <action>
    1. Modificar backend/app/tasks/polling.py — enganchar las alerts tasks en
       _ping_and_update(). CRITICO: no romper los 11 tests existentes.

       Agregar import al inicio del modulo (despues de los imports existentes):
       ```python
       # Import diferido en la funcion para evitar import circular durante tests
       # from app.tasks.alerts import handle_device_down, handle_device_recovery
       ```

       Modificar _ping_and_update() — agregar el disparo de tasks DESPUES del bloque
       `async with AsyncSessionLocal() as db:` que ya existe, justo antes del
       `# Publicar a Redis solo si el status cambio`. Reemplazar el comentario y el
       bloque de publicacion por:

       ```python
       # Publicar a Redis solo si el status cambio
       if new_status != previous_status:
           await publish_status_update(device.id, new_status.value)

           # ALERT-01: disparar tasks de alerta en transiciones de estado
           # Import diferido aqui para evitar import circular en tests
           from app.tasks.alerts import handle_device_down, handle_device_recovery

           if new_status == DeviceStatus.DOWN:
               # ALERT-04: countdown = ALERT_DEBOUNCE_SECONDS (debounce anti-flapping)
               # Si el equipo se recupera antes del countdown, handle_device_down
               # encontrara el incidente cerrado y suprimira la alerta
               handle_device_down.apply_async(
                   args=[device.id],
                   countdown=settings.ALERT_DEBOUNCE_SECONDS,
               )
           elif new_status == DeviceStatus.UP and previous_status == DeviceStatus.DOWN:
               # Recovery inmediato — sin countdown
               handle_device_recovery.delay(device.id)
       ```

       CRITICO: Este cambio extiende el codigo DESPUES del bloque db existente y
       de la llamada publish_status_update ya existente. No modificar la logica
       de consecutive_failures ni el bloque db — solo agregar el disparo de alerts.

    2. Modificar backend/app/celery_app.py — agregar "app.tasks.alerts" al include:
       ```python
       include=[
           "app.tasks.polling",
           "app.tasks.mikrotik",
           "app.tasks.alerts",
           # app.tasks.maintenance — se agrega en Plan 03
       ],
       ```

    3. Ejecutar suite completa para confirmar cero regresiones:
       `cd backend && python -m pytest tests/unit/ -x -q`

       Los 11 tests de test_polling.py deben seguir pasando. El import diferido
       de alerts dentro de la funcion evita que los mocks de tests_polling.py
       necesiten conocer el modulo alerts.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/ -x -q</automated>
  </verify>
  <done>
    - polling.py dispara handle_device_down.apply_async(countdown=ALERT_DEBOUNCE_SECONDS) al pasar a DOWN
    - polling.py dispara handle_device_recovery.delay() al pasar DOWN->UP
    - celery_app.py incluye "app.tasks.alerts" en include
    - Suite completa tests/unit/ pasa sin regresiones (minimo 62 tests = 51 previos + nuevos de Plan 01 + Plan 02)
    - Import de alerts es diferido (dentro de _ping_and_update) para compatibilidad con tests existentes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker Celery -> Telegram Bot API | Token sale del worker hacia internet; mensajes contienen nombre de equipo, IP y sitio |
| DB Incidents -> Telegram message | Datos de la DB se incluyen en el mensaje Telegram sin escapado adicional |
| polling.py -> alerts tasks | Celery encola tasks con device_id (int) — superficie de injection minima |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07 | Information Disclosure | `send_telegram_alert` — TELEGRAM_BOT_TOKEN en settings | mitigate | settings.TELEGRAM_BOT_TOKEN es str (no SecretStr) — aceptado porque ya estaba en config.py de Phase 1 como str. No loggear el token. Guard vacio retorna sin error. |
| T-3-08 | Information Disclosure | Mensaje Telegram contiene IP del equipo | accept | El chat_id es privado (chat personal o grupo interno del tecnico). IP de equipos internos aceptable en canal privado de Telegram. |
| T-3-09 | Tampering | Race condition en open_incident_if_not_exists | mitigate | SELECT FOR UPDATE en asyncpg + SQLAlchemy 2.0. El indice parcial idx_incidents_active garantiza que el lock es sobre la fila correcta. |
| T-3-10 | Denial of Service | Muchos equipos caidos simultaneamente saturan Telegram | accept | Para v1 con pocos Mikrotik, riesgo bajo. Telegram Bot API acepta ~30 msg/s. Si hay corte masivo, los tasks Celery se procesan secuencialmente en la cola de alertas. |
| T-3-11 | Spoofing | handle_device_down recibe device_id sin validar | mitigate | La funcion hace SELECT Device WHERE id=device_id — si el device no existe, retorna {"skipped": True}. No ejecuta codigo basado en device_id no verificado. |
| T-3-12 | Information Disclosure | format_down_message — HTML injection si device.name tiene caracteres HTML | accept | Datos vienen de DB interna, insertados por el tecnico autenticado. No hay entrada no confiable en mensajes Telegram. Si en v2 se agregan notas de usuarios externos, aplicar html.escape(). |
</threat_model>

<verification>
Verificacion completa del Plan 02:

```bash
# Desde la raiz del proyecto:

# 1. Confirmar archivos creados
ls backend/app/services/telegram.py
ls backend/app/tasks/alerts.py

# 2. Confirmar que polling.py dispara las alerts tasks
grep -n "handle_device_down\|handle_device_recovery" backend/app/tasks/polling.py

# 3. Confirmar import diferido (no en nivel de modulo)
grep -n "^from app.tasks.alerts\|^import app.tasks.alerts" backend/app/tasks/polling.py
# Debe retornar 0 lineas — el import esta dentro de la funcion

# 4. Confirmar celery_app include
grep -n "app.tasks.alerts" backend/app/celery_app.py

# 5. Confirmar SELECT FOR UPDATE en open_incident_if_not_exists
grep -n "with_for_update" backend/app/tasks/alerts.py

# 6. Suite completa
cd backend && python -m pytest tests/unit/ -v --tb=short
```
</verification>

<success_criteria>
- [ ] format_down_message() produce mensaje HTML con nombre, IP en code, sitio, timestamp UTC
- [ ] format_up_message() formatea duracion: 3661s -> "1h 1m 1s", 90s -> "1m 30s"
- [ ] send_telegram_alert() no hace nada si TELEGRAM_BOT_TOKEN == ""
- [ ] send_telegram_alert() usa `async with Bot(token) as bot:` con parse_mode="HTML"
- [ ] handle_device_down() suprime alerta si incidente ya esta cerrado (debounce ALERT-04)
- [ ] open_incident_if_not_exists() usa .with_for_update() (atomicidad INC-01)
- [ ] close_incident() calcula duration_seconds correctamente (INC-02)
- [ ] alert_sent=True marcado en incidente despues de enviar alerta DOWN (ALERT-05)
- [ ] recovery_alert_sent=True marcado despues de enviar alerta UP (ALERT-05)
- [ ] polling.py dispara handle_device_down.apply_async(countdown=ALERT_DEBOUNCE_SECONDS) al DOWN
- [ ] polling.py dispara handle_device_recovery.delay() al DOWN->UP
- [ ] Import de alerts en polling.py es diferido (dentro de funcion, no nivel modulo)
- [ ] celery_app.py incluye "app.tasks.alerts"
- [ ] tests/unit/ completa sin regresiones
</success_criteria>

<output>
Tras completar todas las tareas, crear:
`.planning/phases/03-mikrotik-alertas-incidentes/03-02-SUMMARY.md`

El SUMMARY debe incluir:
- Frontmatter YAML con: phase, plan, subsystem, tags, dependency_graph, tech_stack, key_files (created/modified), decisions, metrics
- Seccion "What Was Built": telegram.py, alerts.py, modificaciones a polling.py
- Seccion "Interfaces Exported": firmas de handle_device_down, handle_device_recovery, send_telegram_alert, format_down_message, format_up_message
- Seccion "Test Results": conteo de tests
- Seccion "Deviations from Plan" (si aplica)
- Seccion "Known Stubs": documentar si Telegram no se prueba contra API real
- Seccion "Threat Flags": estado de T-3-07 a T-3-12
</output>
