---
phase: 03-mikrotik-alertas-incidentes
plan: 03
type: tdd
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - backend/app/schemas/incident.py
  - backend/app/api/v1/incidents.py
  - backend/app/main.py
  - backend/app/tasks/maintenance.py
  - backend/app/celery_app.py
  - backend/tests/unit/test_incidents.py
  - backend/tests/unit/test_maintenance.py
autonomous: true
requirements:
  - INC-01
  - INC-02
  - INC-03
  - INC-04
  - MK-03

must_haves:
  truths:
    - "GET /api/v1/incidents devuelve lista de incidentes con nombre y sitio del equipo"
    - "El endpoint filtra por device_id y por site cuando se pasan como query params"
    - "El endpoint requiere JWT valido (CurrentUser dependency)"
    - "La tarea Celery beat diaria elimina metricas e incidentes con mas de 30 dias"
    - "La limpieza NO elimina incidentes con resolved_at IS NULL (incidentes abiertos)"
  artifacts:
    - path: "backend/app/schemas/incident.py"
      provides: "IncidentResponse Pydantic schema con device_name y device_site"
      exports: ["IncidentResponse"]
    - path: "backend/app/api/v1/incidents.py"
      provides: "GET /incidents router con filtros device_id y site"
      exports: ["router"]
    - path: "backend/app/tasks/maintenance.py"
      provides: "cleanup_old_data() Celery task con DELETE de 30 dias"
      exports: ["cleanup_old_data"]
    - path: "backend/tests/unit/test_incidents.py"
      provides: "Tests INC-03"
    - path: "backend/tests/unit/test_maintenance.py"
      provides: "Tests INC-04, MK-03 (limpieza metrics)"
  key_links:
    - from: "backend/app/api/v1/incidents.py"
      to: "backend/app/models/incident.py"
      via: "select(Incident, Device.name, Device.site).join(Device)"
      pattern: "join\\(Device"
    - from: "backend/app/api/v1/incidents.py"
      to: "backend/app/api/deps.py"
      via: "CurrentUser dependency en parametros del endpoint"
      pattern: "CurrentUser"
    - from: "backend/app/tasks/maintenance.py"
      to: "metrics y incidents tables"
      via: "DELETE WHERE recorded_at/resolved_at < NOW() - INTERVAL '30 days'"
      pattern: "INTERVAL '30 days'"
    - from: "backend/app/celery_app.py"
      to: "backend/app/tasks/maintenance.py"
      via: "beat_schedule cleanup-old-data con crontab(hour=3, minute=0)"
      pattern: "cleanup-old-data"
---

<objective>
API REST de incidentes + tarea de limpieza automatica de datos historicos.

Purpose: Cierra el loop funcional de Phase 3 — el tecnico puede consultar el historial
de incidentes desde el dashboard, y la DB no crece indefinidamente gracias a la
limpieza diaria de datos con mas de 30 dias.

Output:
- `backend/app/schemas/incident.py`: IncidentResponse Pydantic v2 con JOIN de Device.
- `backend/app/api/v1/incidents.py`: GET /api/v1/incidents con filtros y JWT.
- `backend/app/tasks/maintenance.py`: cleanup_old_data() Celery task.
- `backend/app/celery_app.py` actualizado con beat schedule de limpieza.
- `backend/app/main.py` actualizado con el nuevo router montado.
- Tests cubriendo INC-03, INC-04, MK-03 (limpieza metrics).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mikrotik-alertas-incidentes/03-RESEARCH.md
@.planning/phases/03-mikrotik-alertas-incidentes/03-01-SUMMARY.md
@.planning/phases/03-mikrotik-alertas-incidentes/03-02-SUMMARY.md

<interfaces>
<!-- Interfaces existentes que el executor necesita. Extraidas del codebase. -->

De backend/app/models/incident.py:
```python
class Incident(Base):
    __tablename__ = "incidents"
    id: Mapped[int]
    device_id: Mapped[int]
    started_at: Mapped[datetime]
    resolved_at: Mapped[datetime | None]
    duration_seconds: Mapped[int | None]
    cause: Mapped[str | None]
    alert_sent: Mapped[bool]
    recovery_alert_sent: Mapped[bool]
```

De backend/app/models/device.py:
```python
class Device(Base):
    __tablename__ = "devices"
    id: Mapped[int]
    name: Mapped[str]
    site: Mapped[str | None]
    ip_address: Mapped[str]
    device_type: Mapped[DeviceType]
    status: Mapped[DeviceStatus]
```

De backend/app/api/deps.py:
```python
# JWT authentication dependency — usar en todos los endpoints
CurrentUser = Annotated[User, Depends(get_current_active_user)]
```

De backend/app/core/database.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]:
    """Dependency FastAPI para inyectar AsyncSession en routers."""
    # usar como: db: AsyncSession = Depends(get_db)
```

De backend/app/main.py (patron existente para montar routers):
```python
# Patron para agregar router en main.py:
app.include_router(devices_router, prefix="/api/v1", tags=["devices"])
# Agregar similar para incidents router
```

De backend/app/celery_app.py (estado al final de Plan 02):
```python
include=[
    "app.tasks.polling",
    "app.tasks.mikrotik",
    "app.tasks.alerts",
    # app.tasks.maintenance — pendiente Plan 03
],
beat_schedule={
    "poll-all-devices": {...},
    "poll-mikrotik-devices": {...},
    # "cleanup-old-data": {...} — pendiente Plan 03
}
```

Patron de esquemas Pydantic v2 establecido (device.py):
```python
class DeviceRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: int
    name: str
    ...
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Tarea 1: Schema IncidentResponse + endpoint GET /incidents con filtros</name>
  <files>
    backend/app/schemas/incident.py,
    backend/app/api/v1/__init__.py,
    backend/app/api/v1/incidents.py,
    backend/app/main.py,
    backend/tests/unit/test_incidents.py
  </files>
  <behavior>
    - Test: GET /incidents retorna lista con los campos: id, device_id, device_name, device_site, started_at, resolved_at, duration_seconds, alert_sent
    - Test: GET /incidents?device_id=5 filtra solo incidentes del dispositivo 5
    - Test: GET /incidents?site=Torre+Norte filtra incidentes de dispositivos en ese sitio
    - Test: GET /incidents?limit=10&offset=20 respeta paginacion
    - Test: GET /incidents ordena por started_at DESC (mas reciente primero)
    - Test: GET /incidents sin token retorna 401
    - Test: IncidentResponse acepta from_attributes=True (row de JOIN SQLAlchemy)
  </behavior>
  <action>
    RED: Crear test_incidents.py con los comportamientos. Usar TestClient de FastAPI
    con override de get_db (AsyncMock que retorna resultados mock). Mockear
    get_current_active_user para el test de autenticacion. Ejecutar — deben fallar.

    GREEN:

    1. backend/app/schemas/incident.py:
       ```python
       """
       Schemas Pydantic v2 para el endpoint de incidentes.
       INC-03: IncidentResponse incluye campos de Device via JOIN.
       """
       from datetime import datetime
       from pydantic import BaseModel, ConfigDict


       class IncidentResponse(BaseModel):
           """
           Schema de respuesta para GET /incidents.
           Los campos device_name y device_site vienen del JOIN con la tabla devices.
           """
           model_config = ConfigDict(from_attributes=True)

           id: int
           device_id: int
           device_name: str
           device_site: str | None
           started_at: datetime
           resolved_at: datetime | None
           duration_seconds: int | None
           alert_sent: bool
           recovery_alert_sent: bool
       ```

    2. backend/app/api/v1/__init__.py — archivo vacio (puede ya existir de Phase 2; si
       existe, no modificar).

    3. backend/app/api/v1/incidents.py:
       ```python
       """
       Router FastAPI para historial de incidentes.
       INC-03: GET /incidents con filtros opcionales por device_id y site.
       Todos los endpoints requieren JWT valido (CurrentUser dependency).
       """
       from typing import Annotated

       from fastapi import APIRouter, Depends, Query
       from sqlalchemy import select
       from sqlalchemy.ext.asyncio import AsyncSession

       from app.api.deps import CurrentUser
       from app.core.database import get_db
       from app.models.device import Device
       from app.models.incident import Incident
       from app.schemas.incident import IncidentResponse

       router = APIRouter(prefix="/incidents", tags=["incidents"])


       @router.get("", response_model=list[IncidentResponse])
       async def list_incidents(
           _: CurrentUser,
           db: Annotated[AsyncSession, Depends(get_db)],
           device_id: int | None = Query(default=None, description="Filtrar por ID de equipo"),
           site: str | None = Query(default=None, description="Filtrar por sitio geografico"),
           limit: int = Query(default=50, ge=1, le=500),
           offset: int = Query(default=0, ge=0),
       ) -> list[IncidentResponse]:
           """
           Lista incidentes con informacion del equipo afectado.

           Filtros opcionales:
           - device_id: solo incidentes del equipo con ese ID
           - site: solo incidentes de equipos en ese sitio geografico
           - limit/offset: paginacion (default 50, max 500)

           Ordena por started_at DESC (mas reciente primero).
           """
           query = (
               select(
                   Incident.id,
                   Incident.device_id,
                   Device.name.label("device_name"),
                   Device.site.label("device_site"),
                   Incident.started_at,
                   Incident.resolved_at,
                   Incident.duration_seconds,
                   Incident.alert_sent,
                   Incident.recovery_alert_sent,
               )
               .join(Device, Incident.device_id == Device.id)
               .order_by(Incident.started_at.desc())
               .limit(limit)
               .offset(offset)
           )

           if device_id is not None:
               query = query.where(Incident.device_id == device_id)
           if site is not None:
               query = query.where(Device.site == site)

           result = await db.execute(query)
           rows = result.mappings().all()
           return [IncidentResponse.model_validate(dict(row)) for row in rows]
       ```

    4. backend/app/main.py — agregar el router de incidents.
       Leer el archivo primero. Agregar despues de la linea que monta devices_router:
       ```python
       from app.api.v1.incidents import router as incidents_router
       app.include_router(incidents_router, prefix="/api/v1")
       ```
       Asegurar que el import esta en la seccion de imports (no en medio del codigo).

    REFACTOR: Verificar que IncidentResponse.model_validate(dict(row)) mapea
    correctamente los labels de la query (device_name, device_site).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_incidents.py -x -q</automated>
  </verify>
  <done>
    - tests/unit/test_incidents.py: todos los tests pasan (INC-03)
    - GET /api/v1/incidents retorna 200 con lista de IncidentResponse
    - Filtros device_id y site aplicados correctamente
    - Paginacion limit/offset funciona
    - Sin JWT retorna 401
    - Orden started_at DESC verificado
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Tarea 2: Task Celery de limpieza + registro en beat schedule</name>
  <files>
    backend/app/tasks/maintenance.py,
    backend/app/celery_app.py,
    backend/tests/unit/test_maintenance.py
  </files>
  <behavior>
    - Test: cleanup_old_data() es un Celery task con name="tasks.cleanup_old_data"
    - Test: ejecuta DELETE en tabla metrics WHERE recorded_at < NOW() - INTERVAL '30 days'
    - Test: ejecuta DELETE en tabla incidents WHERE resolved_at IS NOT NULL AND resolved_at < NOW() - INTERVAL '30 days'
    - Test: NO elimina incidentes con resolved_at IS NULL (abiertos)
    - Test: retorna dict con metrics_deleted y incidents_deleted (rowcount)
    - Test: el beat_schedule de celery_app tiene entrada "cleanup-old-data" con crontab(hour=3, minute=0)
  </behavior>
  <action>
    RED: Crear test_maintenance.py con los comportamientos. Mockear AsyncSessionLocal
    y db.execute() con resultados que tengan .rowcount. Verificar los textos SQL exactos
    en las llamadas a db.execute(). Ejecutar — deben fallar.

    GREEN:

    1. backend/app/tasks/maintenance.py:
       ```python
       """
       Tarea de mantenimiento: limpieza de datos historicos > 30 dias.

       MK-03/INC-04: Celery beat ejecuta esto diariamente a las 3am (hora Colombia).
       Usa DELETE SQL directo — mas eficiente que ORM para borrado masivo.

       CRITICO: Solo elimina incidentes con resolved_at IS NOT NULL.
       Los incidentes abiertos (equipo aun DOWN) nunca se eliminan.
       """
       import asyncio
       import logging

       from celery import shared_task
       from sqlalchemy import text

       from app.core.database import AsyncSessionLocal

       logger = logging.getLogger(__name__)


       @shared_task(name="tasks.cleanup_old_data")
       def cleanup_old_data() -> dict:
           """
           MK-03/INC-04: Elimina metricas e incidentes con mas de 30 dias.
           Ejecutado por Celery beat diariamente.
           Retorna conteo de filas eliminadas para logging.
           """
           return asyncio.run(_cleanup_async())


       async def _cleanup_async() -> dict:
           async with AsyncSessionLocal() as db:
               # MK-03: limpiar metricas > 30 dias
               r_metrics = await db.execute(
                   text("DELETE FROM metrics WHERE recorded_at < NOW() - INTERVAL '30 days'")
               )

               # INC-04: limpiar incidentes RESUELTOS > 30 dias
               # CRITICO: WHERE resolved_at IS NOT NULL — no tocar incidentes abiertos
               r_incidents = await db.execute(
                   text(
                       "DELETE FROM incidents "
                       "WHERE resolved_at IS NOT NULL "
                       "AND resolved_at < NOW() - INTERVAL '30 days'"
                   )
               )

               await db.commit()

           metrics_deleted = r_metrics.rowcount
           incidents_deleted = r_incidents.rowcount

           logger.info(
               "cleanup_old_data: deleted %s metrics rows, %s incident rows",
               metrics_deleted,
               incidents_deleted,
           )
           return {
               "metrics_deleted": metrics_deleted,
               "incidents_deleted": incidents_deleted,
           }
       ```

    2. backend/app/celery_app.py — agregar "app.tasks.maintenance" al include y
       la entrada de beat schedule. Leer el archivo primero para no perder el estado
       del Plan 01 (mikrotik) y Plan 02 (alerts):

       En la lista include, cambiar el comentario pendiente a entrada real:
       ```python
       include=[
           "app.tasks.polling",
           "app.tasks.mikrotik",
           "app.tasks.alerts",
           "app.tasks.maintenance",
       ],
       ```

       En beat_schedule, agregar al final del dict:
       ```python
       "cleanup-old-data": {
           "task": "tasks.cleanup_old_data",
           "schedule": crontab(hour=3, minute=0),  # 3am hora Colombia (UTC-5)
           "options": {"expires": 3600},  # si no se ejecuto en 1h, descartar
       },
       ```

       Agregar import al inicio de celery_app.py (despues de los imports existentes):
       ```python
       from celery.schedules import crontab
       ```

    3. Ejecutar suite completa:
       `cd backend && python -m pytest tests/unit/ -x -q`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_maintenance.py tests/unit/ -x -q</automated>
  </verify>
  <done>
    - tests/unit/test_maintenance.py: todos los tests pasan (INC-04, MK-03)
    - cleanup_old_data() registrado como Celery task name="tasks.cleanup_old_data"
    - DELETE metrics usa WHERE recorded_at < NOW() - INTERVAL '30 days'
    - DELETE incidents usa WHERE resolved_at IS NOT NULL AND resolved_at < NOW() - INTERVAL '30 days'
    - celery_app.py incluye "app.tasks.maintenance" en include
    - beat_schedule tiene "cleanup-old-data" con crontab(hour=3, minute=0)
    - Suite completa tests/unit/ pasa sin regresiones
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> GET /api/v1/incidents | Query params device_id (int) y site (str) entran al endpoint desde el cliente |
| Celery beat -> DB | La task de limpieza ejecuta DELETE masivo — error de logica borraria datos correctos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-13 | Elevation of Privilege | GET /incidents — sin JWT retorna datos | mitigate | CurrentUser dependency en parametros del endpoint garantiza que FastAPI rechaza con 401 si no hay Bearer token. Verificado en test. |
| T-3-14 | Information Disclosure | GET /incidents expone IPs y nombres de equipos | accept | La plataforma es interna — solo el tecnico autenticado accede. JWT valida la sesion. |
| T-3-15 | Tampering | site parameter en GET /incidents — SQL injection via ORM | mitigate | SQLAlchemy usa parametros enlazados (prepared statements) para todos los .where() — no hay interpolacion de strings en SQL. Pydantic valida tipos (str para site, int para device_id). |
| T-3-16 | Denial of Service | GET /incidents sin limit — retorna todas las filas | mitigate | Parametro limit con ge=1, le=500 en Query(). Default 50. No hay path para retornar N ilimitado. |
| T-3-17 | Tampering | cleanup_old_data — DELETE masivo con logica incorrecta borra datos validos | mitigate | Test verifica que el WHERE incluye `resolved_at IS NOT NULL`. Incidentes abiertos (NULL) nunca se borran. Celery beat options expires=3600 evita ejecuciones acumuladas. |
| T-3-18 | Repudiation | cleanup_old_data — no hay log de cuantas filas se borraron | mitigate | La tarea retorna dict con metrics_deleted e incidents_deleted, y los loggea con logger.info(). El resultado queda en el backend Celery Redis. |
</threat_model>

<verification>
Verificacion completa del Plan 03 (y de toda la Phase 3):

```bash
# Desde la raiz del proyecto:

# 1. Confirmar archivos creados
ls backend/app/schemas/incident.py
ls backend/app/api/v1/incidents.py
ls backend/app/tasks/maintenance.py

# 2. Confirmar que el endpoint esta montado en main.py
grep -n "incidents" backend/app/main.py

# 3. Confirmar cleanup SQL critico (resolved_at IS NOT NULL)
grep -n "resolved_at IS NOT NULL" backend/app/tasks/maintenance.py

# 4. Confirmar crontab en celery_app.py
grep -n "cleanup-old-data\|crontab" backend/app/celery_app.py

# 5. Confirmar que el include tiene los 4 modulos de tasks
grep -A 6 "include=" backend/app/celery_app.py

# 6. Suite completa de toda la Phase 3
cd backend && python -m pytest tests/unit/ -v --tb=short

# 7. Verificar cobertura de requirements Phase 3
cd backend && python -m pytest tests/unit/ -v --tb=short -k "mikrotik or circuit_breaker or thresholds or telegram or alerts or incidents or maintenance"
```
</verification>

<success_criteria>
- [ ] IncidentResponse schema tiene: id, device_id, device_name, device_site, started_at, resolved_at, duration_seconds, alert_sent, recovery_alert_sent
- [ ] GET /api/v1/incidents retorna 401 sin JWT
- [ ] GET /api/v1/incidents?device_id=5 filtra correctamente
- [ ] GET /api/v1/incidents?site=Torre+Norte filtra correctamente
- [ ] GET /api/v1/incidents ordena por started_at DESC
- [ ] cleanup_old_data() DELETE metrics WHERE recorded_at < NOW() - INTERVAL '30 days'
- [ ] cleanup_old_data() DELETE incidents WHERE resolved_at IS NOT NULL AND resolved_at < NOW() - INTERVAL '30 days'
- [ ] cleanup_old_data() NO borra incidentes con resolved_at IS NULL
- [ ] celery_app.py include tiene los 4 modulos: polling, mikrotik, alerts, maintenance
- [ ] beat_schedule tiene "cleanup-old-data" con crontab(hour=3, minute=0)
- [ ] Suite completa tests/unit/ pasa (todos los tests de Phase 2 + Phase 3)

Cobertura de requirements Phase 3 completa:
- [ ] MK-01: cpu_pct y ram_pct escritos en metrics (test_mikrotik.py)
- [ ] MK-02: tx_bps y rx_bps por interfaz en metrics (test_mikrotik.py)
- [ ] MK-03: api.close() en finally + cleanup 30 dias (test_mikrotik.py + test_maintenance.py)
- [ ] MK-04: circuit breaker abre tras 3 fallos, TTL 5min (test_circuit_breaker.py)
- [ ] ALERT-01: polling.py dispara handle_device_down al DOWN (test_polling.py extendido)
- [ ] ALERT-02: mensaje DOWN con nombre/IP/sitio/timestamp (test_telegram.py)
- [ ] ALERT-03: mensaje UP con duracion calculada (test_telegram.py)
- [ ] ALERT-04: debounce — no alerta si incidente cerrado antes del countdown (test_alerts.py)
- [ ] ALERT-05: alert_sent y recovery_alert_sent marcados en incidente (test_alerts.py)
- [ ] ALERT-06: get_threshold() prioriza DB > env var (test_thresholds.py)
- [ ] INC-01: incidente abierto con started_at al DOWN (test_alerts.py)
- [ ] INC-02: incidente cerrado con duration_seconds al UP (test_alerts.py)
- [ ] INC-03: GET /incidents con filtros (test_incidents.py)
- [ ] INC-04: cleanup automatico incidentes > 30 dias (test_maintenance.py)
</success_criteria>

<output>
Tras completar todas las tareas, crear:
`.planning/phases/03-mikrotik-alertas-incidentes/03-03-SUMMARY.md`

El SUMMARY debe incluir:
- Frontmatter YAML con: phase, plan, subsystem, tags, dependency_graph, tech_stack, key_files (created/modified), decisions, metrics
- Seccion "What Was Built": incidents.py router, maintenance.py task, schema IncidentResponse
- Seccion "Endpoints Created": tabla con metodo, ruta, descripcion, status code
- Seccion "Interfaces Exported": IncidentResponse, cleanup_old_data
- Seccion "Test Results": conteo total de tests de Phase 3 completa
- Seccion "Phase 3 Requirements Coverage": tabla MK-*, ALERT-*, INC-* con status
- Seccion "Deviations from Plan" (si aplica)
- Seccion "Known Stubs": ninguno esperado
- Seccion "Threat Flags": estado de T-3-13 a T-3-18
</output>
