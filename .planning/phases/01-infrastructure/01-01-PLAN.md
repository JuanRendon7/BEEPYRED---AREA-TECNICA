---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Dockerfile
  - backend/requirements.txt
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/core/__init__.py
  - backend/app/core/config.py
  - backend/app/celery_app.py
  - backend/scripts/start-worker.sh
  - backend/railway.toml
  - .env.example
  - .gitignore
  - backend/tests/unit/test_no_secrets.py
  - backend/tests/unit/test_vpn_health.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "El repo tiene estructura monorepo /backend con todas las carpetas requeridas creadas"
    - "El Dockerfile construye sin errores y tiene stage base compartido y stage worker con Tailscale"
    - "backend/app/core/config.py usa pydantic-settings — ninguna credencial hardcodeada en el código"
    - "scripts/start-worker.sh inicializa tailscaled en userspace y luego lanza celery"
    - "railway.toml define el servicio web con healthcheck /health; worker y beat se configuran via Railway dashboard"
    - "El archivo .env.example documenta todas las variables requeridas incluyendo TAILSCALE_AUTH_KEY y CUSTOM_DOMAIN"
    - "Ningún valor real de credencial existe en el repositorio git"
  artifacts:
    - path: "backend/Dockerfile"
      provides: "Imagen Docker multi-stage (base + worker con Tailscale)"
      contains: "TS_USERSPACE"
    - path: "backend/app/core/config.py"
      provides: "Settings pydantic-settings con todas las env vars"
      exports: ["settings", "Settings"]
    - path: "backend/scripts/start-worker.sh"
      provides: "Script de arranque Tailscale userspace + Celery worker"
      contains: "tailscaled --tun=userspace-networking"
    - path: "backend/railway.toml"
      provides: "Configuración Railway del servicio web"
      contains: "healthcheckPath"
    - path: ".env.example"
      provides: "Template de variables de entorno sin valores reales"
      contains: "TAILSCALE_AUTH_KEY"
    - path: "backend/tests/unit/test_no_secrets.py"
      provides: "Stub unitario INFRA-03: escanea archivos Python por credenciales hardcodeadas"
      contains: "test_no_secrets"
    - path: "backend/tests/unit/test_vpn_health.py"
      provides: "Stub unitario INFRA-01: verifica que el script start-worker.sh contiene las directivas Tailscale correctas"
      contains: "test_vpn_health"
  key_links:
    - from: "backend/app/core/config.py"
      to: ".env.example"
      via: "pydantic-settings lee las mismas variables documentadas en .env.example"
      pattern: "class Settings.*BaseSettings"
    - from: "backend/scripts/start-worker.sh"
      to: "TAILSCALE_AUTH_KEY env var"
      via: "tailscale up --authkey"
      pattern: "TAILSCALE_AUTH_KEY"
    - from: "backend/Dockerfile"
      to: "backend/scripts/start-worker.sh"
      via: "CMD ['/start-worker.sh'] en el stage worker"
      pattern: "start-worker.sh"
---

<objective>
Construir el esqueleto completo del repositorio monorepo y la infraestructura de conectividad VPN.

Esto incluye:
1. Estructura de directorios del monorepo con /backend y carpetas internas de FastAPI
2. Dockerfile multi-stage con stage base (web) y stage worker (con Tailscale userspace para VPN a la red ISP)
3. Configuración pydantic-settings con todas las variables de entorno requeridas
4. Script de arranque del worker que inicia tailscaled en modo userspace (TS_USERSPACE=1, SOCKS5 en localhost:1055) y luego Celery
5. railway.toml para el servicio web; los servicios worker y beat usan el mismo Dockerfile con diferente startCommand en Railway dashboard
6. .env.example completo como template de variables
7. Stubs de tests unitarios para INFRA-03 (no secrets) e INFRA-01 (VPN health)

Purpose: Sin esta base, ningún componente posterior puede existir. El Dockerfile y la configuración de entorno son la interfaz entre el código local y Railway.

Output:
- backend/ con estructura completa de carpetas y archivos __init__.py
- backend/Dockerfile multi-stage
- backend/requirements.txt con versiones exactas verificadas
- backend/app/core/config.py con Settings pydantic-settings
- backend/app/main.py con FastAPI app y endpoint /health
- backend/app/celery_app.py con instancia Celery configurada
- backend/scripts/start-worker.sh script Tailscale + Celery
- backend/railway.toml configuración servicio web
- .env.example con todas las variables documentadas
- .gitignore con .env excluido
- backend/tests/unit/test_no_secrets.py stub (INFRA-03)
- backend/tests/unit/test_vpn_health.py stub (INFRA-01)

NOTA VPN (DECISIÓN BLOQUEADA): El técnico confirmó Tailscale SaaS como mecanismo VPN.
Railway bloquea NET_ADMIN por lo que WireGuard (kernel-mode, boringtun, wireguard-go) es imposible.
Se usa Tailscale en modo userspace (TS_USERSPACE=1, --tun=userspace-networking) con SOCKS5 en localhost:1055.
El Mikrotik con IP pública fija actúa como Tailscale subnet router anunciando rutas ISP.
Variable de entorno: TAILSCALE_AUTH_KEY (ephemeral key del panel Tailscale).
Esta decisión está bloqueada — no requiere confirmación adicional.

NOTA — DEPLOY-02 (dominio personalizado): La implementación DNS real es Phase 6 per ROADMAP. En este plan se documenta el CUSTOM_DOMAIN en .env.example y se agrega la variable a Settings como preparación. La configuración DNS en Railway dashboard se ejecuta en Phase 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/1-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold del repositorio y Dockerfile multi-stage</name>
  <files>
    backend/Dockerfile,
    backend/requirements.txt,
    backend/app/__init__.py,
    backend/app/main.py,
    backend/app/core/__init__.py,
    backend/app/models/__init__.py,
    backend/app/api/__init__.py,
    backend/app/collectors/__init__.py,
    backend/app/tasks/__init__.py,
    backend/app/celery_app.py,
    backend/scripts/start-worker.sh,
    backend/.dockerignore,
    backend/tests/__init__.py,
    backend/tests/unit/__init__.py,
    backend/tests/unit/test_no_secrets.py,
    backend/tests/unit/test_vpn_health.py,
    .gitignore
  </files>

  <read_first>
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 4: Railway Multi-Service, Pattern 5: Tailscale Userspace en Dockerfile)
    - .planning/phases/01-infrastructure/1-CONTEXT.md (sección Estructura del Repositorio y Estrategia de Deploy)
  </read_first>

  <action>
Crear la estructura de directorios y archivos del monorepo backend.

**1. Crear estructura de carpetas** (usando `mkdir -p` o herramientas de escritura de archivos):
```
backend/
├── app/
│   ├── __init__.py          (vacío)
│   ├── main.py
│   ├── celery_app.py
│   ├── api/
│   │   └── __init__.py      (vacío)
│   ├── collectors/
│   │   └── __init__.py      (vacío)
│   ├── core/
│   │   └── __init__.py      (vacío)
│   ├── models/
│   │   └── __init__.py      (vacío)
│   └── tasks/
│       └── __init__.py      (vacío)
├── scripts/
│   └── start-worker.sh
├── tests/
│   ├── __init__.py          (vacío)
│   └── unit/
│       ├── __init__.py      (vacío)
│       ├── test_no_secrets.py
│       └── test_vpn_health.py
├── .dockerignore
├── Dockerfile
└── requirements.txt
```

**2. backend/requirements.txt** — usar versiones exactas verificadas (2026-04-25):
```
fastapi==0.136.1
uvicorn[standard]==0.46.0
gunicorn==22.0.0
pydantic==2.13.3
pydantic-settings==2.10.1
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
alembic==1.18.4
celery[redis]==5.6.3
redis==7.4.0
cryptography==47.0.0
python-dotenv==1.2.2
httpx==0.28.1
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.0
```

**3. backend/app/main.py** — FastAPI app mínima con health endpoint:
```python
from fastapi import FastAPI

app = FastAPI(title="BEEPYRED NOC", version="1.0.0")


@app.get("/health")
async def health_check() -> dict:
    """Health check endpoint — requerido por railway.toml healthcheckPath."""
    return {"status": "ok", "service": "web"}
```

**4. backend/app/celery_app.py** — instancia Celery (broker y backend desde settings):
```python
from celery import Celery
from app.core.config import settings

celery_app = Celery(
    "beepyred_noc",
    broker=settings.REDIS_URL,
    backend=settings.REDIS_URL,
    include=["app.tasks.maintenance"],
)

celery_app.conf.update(
    task_serializer="json",
    accept_content=["json"],
    result_serializer="json",
    timezone="America/Bogota",
    enable_utc=True,
    # Celery beat: schedule de mantenimiento (Phase 2+)
    beat_schedule={},
)
```

**5. backend/scripts/start-worker.sh** — arranque Tailscale userspace + Celery:
```bash
#!/bin/bash
set -e

echo "[start-worker] Iniciando tailscaled en modo userspace..."
# TS_USERSPACE=1 no requiere NET_ADMIN — compatible con Railway
# DECISIÓN BLOQUEADA: Tailscale userspace confirmado por el técnico de BEEPYRED
tailscaled --tun=userspace-networking --socks5-server=localhost:1055 --state=/tmp/tailscale-state &
TAILSCALE_PID=$!

echo "[start-worker] Esperando a que tailscaled arranque..."
sleep 3

echo "[start-worker] Autenticando con Tailscale..."
tailscale up \
  --authkey="${TAILSCALE_AUTH_KEY}" \
  --hostname="beepyred-noc-worker" \
  --accept-routes

echo "[start-worker] Tailscale conectado. Exportando proxy SOCKS5..."
export ALL_PROXY=socks5://localhost:1055
export HTTP_PROXY=socks5://localhost:1055
export HTTPS_PROXY=socks5://localhost:1055

echo "[start-worker] Iniciando Celery worker..."
exec celery -A app.celery_app worker --loglevel=info --concurrency=4
```
Hacer ejecutable: `chmod +x backend/scripts/start-worker.sh`

**6. backend/Dockerfile** — multi-stage: base compartido + stage worker con Tailscale:
```dockerfile
# ─── Stage base: compartido por web y worker ─────────────────────────────────
FROM python:3.12-slim AS base

WORKDIR /app

# Dependencias del sistema mínimas
RUN apt-get update && apt-get install -y \
    curl \
    iputils-ping \
    && rm -rf /var/lib/apt/lists/*

# Instalar dependencias Python
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# ─── Stage web: FastAPI + Uvicorn ─────────────────────────────────────────────
FROM base AS web
EXPOSE 8000
CMD ["gunicorn", "app.main:app", "-w", "2", "-k", "uvicorn.workers.UvicornWorker", "--bind", "0.0.0.0:8000"]

# ─── Stage worker: Celery + Tailscale userspace VPN ──────────────────────────
FROM base AS worker

# Instalar Tailscale — userspace mode (TS_USERSPACE=1), no requiere NET_ADMIN
# DECISIÓN BLOQUEADA: Tailscale SaaS confirmado. Auth via TAILSCALE_AUTH_KEY env var.
RUN curl -fsSL https://tailscale.com/install.sh | sh

COPY scripts/start-worker.sh /start-worker.sh
RUN chmod +x /start-worker.sh

CMD ["/start-worker.sh"]

# ─── Stage beat: Celery beat scheduler ──────────────────────────────────────
FROM base AS beat
CMD ["celery", "-A", "app.celery_app", "beat", "--loglevel=info", "--scheduler", "celery.beat:PersistentScheduler"]
```

**7. backend/.dockerignore**:
```
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.env
.env.*
!.env.example
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
alembic/versions/
```

**8. .gitignore** (raíz del proyecto):
```
# Entorno Python
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Variables de entorno — NUNCA commitear valores reales
.env
.env.local
.env.production
backend/.env

# pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Node (frontend Phase 2+)
node_modules/
frontend/dist/
frontend/.vite/

# Railway / Docker
.railway/

# OS
.DS_Store
Thumbs.db
```

**9. backend/tests/__init__.py** — vacío.

**10. backend/tests/unit/__init__.py** — vacío.

**11. backend/tests/unit/test_no_secrets.py** — stub INFRA-03: escanea archivos Python por credenciales hardcodeadas.
Este archivo es un stub que Plan 02 puede ampliar. Implementar el contenido completo aquí:
```python
"""
Tests unitarios INFRA-03 — verificar que no hay credenciales hardcodeadas en el código.

Escanea archivos Python del proyecto buscando patrones de secrets comunes:
- Tailscale auth keys (tskey-)
- Fernet tokens (gAAAAA)
- IPs privadas hardcodeadas en código fuente (no en .env.example)
- Strings que parecen passwords largos asignados directamente

Estos tests deben correr en CI para prevenir commits accidentales de credenciales.
"""
import os
import re
from pathlib import Path


# Patrones que indican credencial hardcodeada (no un placeholder)
SECRET_PATTERNS = [
    re.compile(r"tskey-auth-[A-Za-z0-9]+"),   # Tailscale auth key real
    re.compile(r"gAAAAA[A-Za-z0-9_\-]{20,}"),  # Fernet token real (base64 URL-safe)
    re.compile(r"sk-[A-Za-z0-9]{20,}"),         # OpenAI API key pattern
]

# Archivos a ignorar en el escaneo (contienen ejemplos/plantillas, no valores reales)
IGNORED_FILES = {
    ".env.example",
    "test_no_secrets.py",  # Este mismo archivo
}


def _get_python_files(root: Path) -> list[Path]:
    """Retorna todos los archivos .py del proyecto excluyendo IGNORED_FILES."""
    files = []
    for path in root.rglob("*.py"):
        if path.name not in IGNORED_FILES and ".venv" not in path.parts:
            files.append(path)
    return files


def test_no_tailscale_auth_key_in_code():
    """Ningún archivo Python contiene una Tailscale auth key real (tskey-auth-...)."""
    project_root = Path(__file__).parent.parent.parent  # backend/
    python_files = _get_python_files(project_root)

    violations = []
    pattern = SECRET_PATTERNS[0]  # tskey-auth-...
    for f in python_files:
        content = f.read_text(encoding="utf-8", errors="ignore")
        if pattern.search(content):
            violations.append(str(f))

    assert not violations, (
        f"Tailscale auth key detectada en archivos de código:\n"
        + "\n".join(violations)
        + "\nMover a variable de entorno TAILSCALE_AUTH_KEY."
    )


def test_no_fernet_token_in_code():
    """Ningún archivo Python contiene un token Fernet real (gAAAAA...)."""
    project_root = Path(__file__).parent.parent.parent  # backend/
    python_files = _get_python_files(project_root)

    violations = []
    pattern = SECRET_PATTERNS[1]  # gAAAAA...
    for f in python_files:
        content = f.read_text(encoding="utf-8", errors="ignore")
        if pattern.search(content):
            violations.append(str(f))

    assert not violations, (
        f"Token Fernet real detectado en archivos de código:\n"
        + "\n".join(violations)
        + "\nMover a variable de entorno FERNET_KEY."
    )


def test_env_example_uses_placeholders():
    """.env.example contiene solo CHANGE_ME placeholders, no valores reales."""
    env_example = Path(__file__).parent.parent.parent.parent / ".env.example"
    if not env_example.exists():
        # Si no existe aún, el test pasa (se creará en Task 2 del plan)
        return

    content = env_example.read_text(encoding="utf-8")

    # Verificar que contiene los marcadores CHANGE_ME
    assert "CHANGE_ME" in content, (
        ".env.example no contiene marcadores CHANGE_ME — puede tener valores reales"
    )

    # Verificar que no contiene auth keys reales
    for pattern in SECRET_PATTERNS:
        match = pattern.search(content)
        assert not match, (
            f".env.example contiene un posible secret real: {match.group()}\n"
            "Reemplazar con CHANGE_ME placeholder."
        )
```

**12. backend/tests/unit/test_vpn_health.py** — stub INFRA-01: verifica directivas Tailscale en el script (sin red real).
```python
"""
Tests unitarios INFRA-01 — verificar configuración Tailscale en start-worker.sh.

Estos tests son de estructura (no de integración): verifican que el script
start-worker.sh contiene las directivas correctas de Tailscale userspace.
No requieren tailscaled corriendo ni acceso a la red ISP.

DECISIÓN BLOQUEADA: Tailscale SaaS con TAILSCALE_AUTH_KEY confirmado por el técnico.
"""
from pathlib import Path


def _get_start_worker_content() -> str:
    """Lee el contenido de start-worker.sh."""
    script = Path(__file__).parent.parent.parent / "scripts" / "start-worker.sh"
    assert script.exists(), (
        f"start-worker.sh no encontrado en {script}\n"
        "Task 1 de Plan 01 debe crear este archivo."
    )
    return script.read_text(encoding="utf-8")


def test_start_worker_uses_tailscale_userspace():
    """start-worker.sh usa --tun=userspace-networking (no kernel mode que requiere NET_ADMIN)."""
    content = _get_start_worker_content()
    assert "--tun=userspace-networking" in content, (
        "start-worker.sh debe usar '--tun=userspace-networking' para Tailscale userspace.\n"
        "Railway bloquea NET_ADMIN — el modo kernel de Tailscale no funciona."
    )


def test_start_worker_uses_tailscale_auth_key_env_var():
    """start-worker.sh autentica con Tailscale usando la variable TAILSCALE_AUTH_KEY (no hardcoded)."""
    content = _get_start_worker_content()
    assert "${TAILSCALE_AUTH_KEY}" in content, (
        "start-worker.sh debe usar ${TAILSCALE_AUTH_KEY} para la autenticación Tailscale.\n"
        "El valor real de la key viene del entorno Railway — nunca hardcodeado."
    )


def test_start_worker_exports_socks5_proxy():
    """start-worker.sh exporta ALL_PROXY=socks5://localhost:1055 para que los collectors lo usen."""
    content = _get_start_worker_content()
    assert "ALL_PROXY=socks5://localhost:1055" in content, (
        "start-worker.sh debe exportar ALL_PROXY=socks5://localhost:1055.\n"
        "Los collectors (librouteros, asyncssh) necesitan este proxy para conectar via Tailscale."
    )


def test_start_worker_launches_celery_worker():
    """start-worker.sh lanza 'celery worker' al final (después de Tailscale)."""
    content = _get_start_worker_content()
    assert "celery" in content and "worker" in content, (
        "start-worker.sh debe lanzar el Celery worker después de inicializar Tailscale."
    )


def test_dockerfile_has_tailscale_in_worker_stage():
    """El stage 'worker' del Dockerfile instala tailscale (no en el stage base ni web)."""
    dockerfile = Path(__file__).parent.parent.parent / "Dockerfile"
    assert dockerfile.exists(), (
        f"Dockerfile no encontrado en {dockerfile}\n"
        "Task 1 de Plan 01 debe crear este archivo."
    )
    content = dockerfile.read_text(encoding="utf-8")

    # Verificar que existe el stage worker
    assert "AS worker" in content, "Dockerfile debe tener un stage 'AS worker'"

    # Verificar que tailscale se instala en el Dockerfile
    assert "tailscale" in content.lower(), (
        "Dockerfile debe instalar tailscale en el stage worker."
    )
```
  </action>

  <verify>
    <automated>
      cd '/c/Users/SAP Developer/Desktop/Proyectos Juan/Área técnica BEEPYRED' && \
      test -f backend/Dockerfile && \
      test -f backend/requirements.txt && \
      test -f backend/app/main.py && \
      test -f backend/app/celery_app.py && \
      test -f backend/app/core/__init__.py && \
      test -f backend/app/models/__init__.py && \
      test -f backend/app/tasks/__init__.py && \
      test -f backend/scripts/start-worker.sh && \
      test -x backend/scripts/start-worker.sh && \
      test -f backend/tests/__init__.py && \
      test -f backend/tests/unit/__init__.py && \
      test -f backend/tests/unit/test_no_secrets.py && \
      test -f backend/tests/unit/test_vpn_health.py && \
      grep -q "TS_USERSPACE\|userspace-networking" backend/scripts/start-worker.sh && \
      grep -q "FROM.*worker" backend/Dockerfile && \
      grep -q "tailscale" backend/Dockerfile && \
      grep -q "FROM.*beat" backend/Dockerfile && \
      grep -q "fastapi==0.136.1" backend/requirements.txt && \
      grep -q "pydantic-settings==2.10.1" backend/requirements.txt && \
      echo "PASS: estructura, Dockerfile y stubs de tests correctos"
    </automated>
  </verify>

  <acceptance_criteria>
    - `backend/Dockerfile` contiene exactamente 4 stages: `FROM.*AS base`, `FROM.*AS web`, `FROM.*AS worker`, `FROM.*AS beat` — verificable con `grep "^FROM" backend/Dockerfile | wc -l` = 4
    - `backend/scripts/start-worker.sh` contiene la cadena `--tun=userspace-networking` (Tailscale userspace mode, no kernel)
    - `backend/scripts/start-worker.sh` contiene la cadena `ALL_PROXY=socks5://localhost:1055`
    - `backend/scripts/start-worker.sh` contiene `${TAILSCALE_AUTH_KEY}` (variable de entorno, no valor hardcodeado)
    - `backend/scripts/start-worker.sh` tiene permisos de ejecución (`test -x backend/scripts/start-worker.sh` retorna 0)
    - `backend/requirements.txt` contiene `pydantic-settings==2.10.1`
    - `backend/requirements.txt` contiene `cryptography==47.0.0`
    - `backend/app/main.py` contiene el endpoint `@app.get("/health")`
    - `backend/app/celery_app.py` contiene `timezone="America/Bogota"`
    - `.gitignore` en la raíz contiene la línea `.env`
    - NO existe ningún archivo `.env` con valores reales (`test ! -f backend/.env` retorna 0)
    - `backend/tests/unit/test_no_secrets.py` existe y contiene `test_no_tailscale_auth_key_in_code`
    - `backend/tests/unit/test_vpn_health.py` existe y contiene `test_start_worker_uses_tailscale_userspace`
  </acceptance_criteria>

  <done>
    El esqueleto del monorepo existe con todos los archivos y carpetas. El Dockerfile construye correctamente los stages base, web, worker (con Tailscale) y beat. El script start-worker.sh inicia tailscaled en modo userspace sin requerir NET_ADMIN, autentica con TAILSCALE_AUTH_KEY y exporta ALL_PROXY. Los stubs de tests unitarios test_no_secrets.py y test_vpn_health.py existen en backend/tests/unit/. Las dependencias Python tienen versiones exactas. .gitignore protege .env de ser commiteado.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: pydantic-settings config, railway.toml y .env.example</name>
  <files>
    backend/app/core/config.py,
    backend/railway.toml,
    .env.example
  </files>

  <read_first>
    - backend/app/core/__init__.py (confirmar que existe antes de crear config.py)
    - .planning/phases/01-infrastructure/01-RESEARCH.md (Pattern 3: pydantic-settings, Pattern 4: Railway Multi-Service)
    - .planning/phases/01-infrastructure/1-CONTEXT.md (sección Gestión de Credenciales)
  </read_first>

  <action>
Crear la configuración centralizada de variables de entorno con pydantic-settings y la configuración de Railway.

**1. backend/app/core/config.py** — Settings con pydantic-settings v2:
```python
"""
Configuración centralizada de BEEPYRED NOC.

Todas las credenciales vienen de variables de entorno — NUNCA hardcodeadas.
Railway provee automáticamente: DATABASE_URL, REDIS_URL, PORT.
Las demás deben configurarse manualmente en Railway dashboard.

IMPORTANTE: Nunca loggear settings.FERNET_KEY ni settings.DATABASE_URL.
"""
from pydantic import SecretStr
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=True,
        # Ignorar variables extra del entorno (Railway inyecta muchas)
        extra="ignore",
    )

    # ── Base de datos ──────────────────────────────────────────────────────────
    # Railway provee DATABASE_URL automáticamente al agregar el addon PostgreSQL
    # Formato Railway: postgresql://user:pass@host:5432/dbname
    # Para asyncpg: reemplazar postgresql:// con postgresql+asyncpg:// en database.py
    DATABASE_URL: str

    # ── Redis ──────────────────────────────────────────────────────────────────
    # Railway provee REDIS_URL automáticamente al agregar el addon Redis
    REDIS_URL: str

    # ── Seguridad ──────────────────────────────────────────────────────────────
    # SECRET_KEY: usado para JWT en Phase 2. Generar con:
    #   python -c "import secrets; print(secrets.token_hex(32))"
    SECRET_KEY: SecretStr

    # FERNET_KEY: cifrado de credenciales de equipos en DB. Generar con:
    #   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
    # CRÍTICO: si se pierde, todas las credenciales almacenadas son irrecuperables.
    FERNET_KEY: SecretStr

    # ── Tailscale VPN (INFRA-01) ───────────────────────────────────────────────
    # Auth key efímera de Tailscale — generar en tailscale.com/admin/settings/keys
    # Solo el servicio worker necesita esta variable.
    # DECISIÓN BLOQUEADA: Tailscale SaaS confirmado por el técnico de BEEPYRED.
    TAILSCALE_AUTH_KEY: str = ""

    # ── Telegram (Phase 3 — incluir ahora para no redesplegar) ────────────────
    TELEGRAM_BOT_TOKEN: str = ""
    TELEGRAM_CHAT_ID: str = ""

    # ── Polling (Phase 2) ──────────────────────────────────────────────────────
    POLL_INTERVAL_SECONDS: int = 60
    MAX_CONCURRENT_CONNECTIONS: int = 50
    DEVICE_TIMEOUT_SECONDS: int = 10

    # ── Umbrales de alerta (Phase 3) ───────────────────────────────────────────
    CPU_ALERT_THRESHOLD_PCT: float = 90.0
    ONU_SIGNAL_MIN_DBM: float = -28.0
    CONSECUTIVE_FAILURES_THRESHOLD: int = 3

    # ── Railway auto-sets PORT ─────────────────────────────────────────────────
    PORT: int = 8000

    # ── Deploy (DEPLOY-02) — dominio personalizado BEEPYRED ───────────────────
    # La configuración DNS real se completa en Phase 6 en el Railway dashboard.
    # Esta variable documenta el dominio objetivo para referencia.
    CUSTOM_DOMAIN: str = ""


settings = Settings()
```

**2. backend/railway.toml** — configuración del servicio web:
```toml
# railway.toml — configura el servicio "web" de BEEPYRED NOC
# Los servicios "worker" y "beat" usan el mismo Dockerfile con distinto startCommand
# en el Railway dashboard (campo "Start Command" de cada servicio).
#
# Start commands por servicio (copiar al crear en Railway dashboard):
#   web:    gunicorn app.main:app -w 2 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
#   worker: /start-worker.sh  (Tailscale userspace + Celery worker, ver Dockerfile stage worker)
#   beat:   celery -A app.celery_app beat --loglevel=info --scheduler celery.beat:PersistentScheduler
#
# Migraciones Alembic: ejecutar como Railway pre-deploy command:
#   cd backend && alembic upgrade head
# (NO en el startup del contenedor web — puede fallar si la DB no está lista)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# Para el servicio web usar el stage "web" del Dockerfile multi-stage
buildTarget = "web"

[deploy]
startCommand = "gunicorn app.main:app -w 2 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
```

**3. .env.example** — template completo sin valores reales (raíz del proyecto):
```bash
# ══════════════════════════════════════════════════════════════════════════════
# BEEPYRED NOC — Variables de Entorno
# Copiar este archivo a .env y completar con valores reales.
# NUNCA commitear el archivo .env con valores reales al repositorio.
# ══════════════════════════════════════════════════════════════════════════════

# ── Base de datos ─────────────────────────────────────────────────────────────
# Railway provee esta variable automáticamente al agregar el addon PostgreSQL.
# Formato: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@localhost:5432/beepyred_noc

# ── Redis ─────────────────────────────────────────────────────────────────────
# Railway provee esta variable automáticamente al agregar el addon Redis.
# Formato: redis://default:password@host:6379
REDIS_URL=redis://localhost:6379

# ── Seguridad ─────────────────────────────────────────────────────────────────
# Generar SECRET_KEY con:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_ME_generate_with_secrets_token_hex_32

# Generar FERNET_KEY con:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# CRÍTICO: Guardar en password manager. Sin esta key las credenciales de equipos son irrecuperables.
FERNET_KEY=CHANGE_ME_generate_with_Fernet_generate_key

# ── Tailscale VPN (INFRA-01) ─────────────────────────────────────────────────
# Obtener en: https://login.tailscale.com/admin/settings/keys
# Crear auth key efímera con "Reusable" desactivado y "Ephemeral" activado para el worker.
# Solo el servicio "worker" de Railway necesita esta variable.
#
# DECISIÓN TÉCNICA CONFIRMADA: Railway bloquea NET_ADMIN — WireGuard (kernel-mode,
# boringtun, wireguard-go) es imposible en Railway. Se usa Tailscale userspace
# (--tun=userspace-networking) con proxy SOCKS5 en localhost:1055.
# El Mikrotik con IP pública fija actúa como Tailscale subnet router anunciando
# las rutas de la red ISP privada. Decisión confirmada por el técnico de BEEPYRED.
TAILSCALE_AUTH_KEY=tskey-auth-CHANGE_ME

# ── Telegram (Phase 3) ───────────────────────────────────────────────────────
# Crear bot con @BotFather en Telegram. Obtener chat_id con @userinfobot.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Polling ───────────────────────────────────────────────────────────────────
POLL_INTERVAL_SECONDS=60
MAX_CONCURRENT_CONNECTIONS=50
DEVICE_TIMEOUT_SECONDS=10

# ── Umbrales de alerta (Phase 3) ─────────────────────────────────────────────
CPU_ALERT_THRESHOLD_PCT=90.0
ONU_SIGNAL_MIN_DBM=-28.0
CONSECUTIVE_FAILURES_THRESHOLD=3

# ── Deploy ────────────────────────────────────────────────────────────────────
# Railway auto-asigna PORT — no cambiar en producción.
PORT=8000

# Dominio personalizado BEEPYRED (configuración DNS en Railway dashboard — Phase 6).
# Ejemplo: noc.beepyred.com
CUSTOM_DOMAIN=
```
  </action>

  <verify>
    <automated>
      cd '/c/Users/SAP Developer/Desktop/Proyectos Juan/Área técnica BEEPYRED' && \
      python3 -c "import ast; ast.parse(open('backend/app/core/config.py').read()); print('config.py: sintaxis OK')" && \
      grep -q "pydantic_settings" backend/app/core/config.py && \
      grep -q "BaseSettings" backend/app/core/config.py && \
      grep -q "FERNET_KEY" backend/app/core/config.py && \
      grep -q "TAILSCALE_AUTH_KEY" backend/app/core/config.py && \
      grep -q "SecretStr" backend/app/core/config.py && \
      grep -q "healthcheckPath" backend/railway.toml && \
      grep -q "buildTarget" backend/railway.toml && \
      grep -q "alembic upgrade head" backend/railway.toml && \
      grep -q "FERNET_KEY" .env.example && \
      grep -q "TAILSCALE_AUTH_KEY" .env.example && \
      grep -q "CHANGE_ME" .env.example && \
      echo "PASS: config.py, railway.toml y .env.example correctos"
    </automated>
  </verify>

  <acceptance_criteria>
    - `backend/app/core/config.py` importa de `pydantic_settings` — verificable con `grep "from pydantic_settings" backend/app/core/config.py`
    - `backend/app/core/config.py` define `FERNET_KEY: SecretStr` y `SECRET_KEY: SecretStr` — verificable con `grep "SecretStr" backend/app/core/config.py | wc -l` >= 2
    - `backend/app/core/config.py` contiene `TAILSCALE_AUTH_KEY: str = ""` (con default vacío) — verificable con `grep 'TAILSCALE_AUTH_KEY.*str.*=.*""' backend/app/core/config.py`
    - `backend/app/core/config.py` NO contiene ninguna credencial real
    - `backend/railway.toml` contiene `healthcheckPath = "/health"` — verificable con `grep 'healthcheckPath = "/health"' backend/railway.toml`
    - `backend/railway.toml` contiene `buildTarget = "web"` — verificable con `grep 'buildTarget' backend/railway.toml`
    - `backend/railway.toml` contiene comentario sobre `alembic upgrade head` como pre-deploy command — verificable con `grep "alembic upgrade head" backend/railway.toml`
    - `.env.example` contiene la línea `TAILSCALE_AUTH_KEY=tskey-auth-CHANGE_ME` — verificable con `grep "TAILSCALE_AUTH_KEY=tskey-auth-CHANGE_ME" .env.example`
    - `.env.example` contiene explicación de por qué se usa Tailscale userspace (menciona NET_ADMIN y WireGuard) — verificable con `grep -i "NET_ADMIN\|WireGuard" .env.example`
    - `.env.example` contiene `CUSTOM_DOMAIN=` — verificable con `grep "^CUSTOM_DOMAIN=" .env.example`
  </acceptance_criteria>

  <done>
    backend/app/core/config.py con Settings pydantic-settings contiene todas las variables de entorno requeridas. Ninguna credencial está hardcodeada. railway.toml configura el servicio web con healthcheck y documenta el comando alembic upgrade head como pre-deploy. .env.example documenta todas las variables con instrucciones claras, incluyendo la explicación técnica de por qué se usa Tailscale (decisión bloqueada confirmada por el técnico).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → código | Variables de entorno cruzando al proceso Python — si .env se commitea, credenciales expuestas |
| Railway container → red ISP | El worker cruza hacia la red privada del ISP via Tailscale SOCKS5 — tráfico no auditado por Railway |
| Dockerfile → imagen pública | Si la imagen Docker se publica, secrets en capas de imagen serían expuestos |
| app/core/config.py → logs | Settings puede loggear valores si se usa str() en lugar de SecretStr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | .env / .gitignore | mitigate | .gitignore excluye .env y .env.*; .env.example usa CHANGE_ME placeholders; test_no_secrets.py en CI detecta leaks de tskey- y tokens Fernet |
| T-1-02 | Information Disclosure | app/core/config.py | mitigate | FERNET_KEY y SECRET_KEY son `SecretStr` — pydantic no los imprime en repr() ni en logs automáticos; nunca usar `str(settings.FERNET_KEY)` en logs |
| T-1-03 | Elevation of Privilege | Tailscale TAILSCALE_AUTH_KEY | mitigate | Usar auth key de tipo "ephemeral" en Tailscale admin (se invalida al desconectarse); no usar reusable keys de larga duración; rotar auth key con cada deploy |
| T-1-04 | Tampering | Dockerfile sin pinning de tailscale | accept | `curl ... tailscale.com/install.sh | sh` descarga versión más reciente — riesgo de supply chain bajo (Tailscale es empresa reputada); alternativa: fijar versión en Dockerfile (mitigación futura si el riesgo escala) |
| T-1-05 | Information Disclosure | Logs del worker | mitigate | El script start-worker.sh usa `echo` para debugging — no incluir `echo $TAILSCALE_AUTH_KEY` ni variables secretas en ningún echo; los logs van a Railway y son visibles para todos los usuarios con acceso al proyecto |
| T-1-06 | Denial of Service | Tailscale SaaS dependency | accept | Si tailscale.com tiene outage, el worker no puede conectar a la red ISP; riesgo aceptado — el NOC seguirá corriendo pero sin poder alcanzar equipos; fallback manual: worker on-premises |
</threat_model>

<verification>
Verificación de Phase 1 Plan 01 completa cuando:
1. `find backend/ -name "__init__.py" | wc -l` retorna >= 5 (todos los paquetes Python tienen __init__.py)
2. `grep "FROM.*AS" backend/Dockerfile | wc -l` retorna 4 (base, web, worker, beat)
3. `grep "tailscaled --tun=userspace-networking" backend/scripts/start-worker.sh` retorna la línea
4. `grep "FERNET_KEY.*SecretStr" backend/app/core/config.py` retorna la línea
5. `grep "^\.env$" .gitignore` retorna la línea (protección del .env real)
6. `grep "CHANGE_ME" .env.example | wc -l` retorna >= 2 (placeholders visibles)
7. `test ! -f backend/.env` — no existe archivo .env con credenciales reales
8. `test -f backend/tests/unit/test_no_secrets.py` — stub INFRA-03 existe
9. `test -f backend/tests/unit/test_vpn_health.py` — stub INFRA-01 existe
</verification>

<success_criteria>
- El repositorio tiene estructura monorepo /backend con todos los __init__.py creados
- El Dockerfile construye exitosamente con `docker build --target web -t beepyred-noc:web backend/`
- El stage worker del Dockerfile instala Tailscale y usa start-worker.sh con userspace networking
- backend/app/core/config.py usa pydantic-settings v2 con TAILSCALE_AUTH_KEY (decisión bloqueada: Tailscale SaaS)
- FERNET_KEY y SECRET_KEY son SecretStr — nunca aparecen en logs de forma accidental
- .env.example documenta todas las variables con instrucciones de generación y sin valores reales
- .gitignore protege .env de ser commiteado
- railway.toml tiene healthcheckPath = "/health" y documenta alembic upgrade head como pre-deploy
- backend/tests/unit/test_no_secrets.py y test_vpn_health.py existen con tests estructurales
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/01-infrastructure/01-01-SUMMARY.md` con:
- Archivos creados y su propósito
- Decisión VPN confirmada: Tailscale SaaS con TAILSCALE_AUTH_KEY (decisión bloqueada)
- Versiones de librerías usadas
- Variables de entorno requeridas y cómo generarlas
- Resultado de los tests estructurales (pytest tests/unit/test_vpn_health.py)
</output>
